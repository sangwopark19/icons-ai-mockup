---
phase: 04-api-key-management
plan: 05
type: execute
wave: 2
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [KEY-01, KEY-02, KEY-03, KEY-04, KEY-05, KEY-06]

must_haves:
  truths:
    - "All unit tests pass (crypto + AdminService)"
    - "TypeScript compiles clean across both apps"
    - "API key management UI verified by human"
    - "Dashboard activeApiKeys field shows real data (not null)"
  artifacts: []
  key_links: []
---

<objective>
Final verification checkpoint: run full test suite, typecheck, and get human visual approval of the API key management flow.

Purpose: Confirm all KEY-01 through KEY-06 requirements are met end-to-end before declaring Phase 4 complete.

Output: Phase 4 verified and ready for completion.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-api-key-management/04-CONTEXT.md
@.planning/phases/04-api-key-management/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and typecheck</name>
  <files></files>
  <action>
Run the complete verification suite:

1. `cd apps/api && npm test` — all unit tests must pass (crypto + AdminService including Phase 1-3 tests)
2. `cd apps/api && npx tsc --noEmit` — API TypeScript clean
3. `cd apps/web && npx tsc --noEmit` or project-level `npm run typecheck` — Web TypeScript clean
4. Verify no `process.env.GEMINI_API_KEY` references in gemini.service.ts: `grep "process.env.GEMINI\|config.gemini" apps/api/src/services/gemini.service.ts` should return nothing
5. Verify encryptedKey not leaked: `grep "encryptedKey" apps/api/src/routes/admin/api-keys.routes.ts` should return nothing (routes should not reference it)

Report any failures for human review.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/icons-ai-mockup/apps/api && npm test && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. TypeScript clean. No env var fallback. No encryptedKey leak.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of API key management flow</name>
  <files></files>
  <action>
Present the verification checklist to the user. The system has been fully built:
- Backend: Prisma ApiKey model, AES-256-GCM crypto, AdminService CRUD, REST API routes
- Frontend: API key table page with add/delete/activate modals and toast notifications
- GeminiService refactored to use DB-stored keys instead of env var
- Worker fetches active key per job, increments call count

Steps for user to verify:
1. Start dev servers: `cd apps/api && npm run dev` and `cd apps/web && npm run dev`
2. Log in as admin and navigate to /admin/api-keys
3. Verify empty state shows message
4. Click "키 추가" -> enter alias + API key -> submit
5. Verify key appears in table with correct columns
6. Add a second key, activate it, verify badge switching
7. Verify active key has no delete/activate buttons
8. Delete inactive key, verify removal
9. Check dashboard shows active key info
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>Human confirms all KEY-01 through KEY-06 requirements are met visually and functionally.</done>
</task>

</tasks>

<verification>
- All automated tests GREEN
- TypeScript clean
- Human visual verification of the complete API key management flow
</verification>

<success_criteria>
- Full test suite passes
- TypeScript compiles clean
- Human confirms: table renders correctly, add/delete/activate work, badges correct, toast notifications appear, dashboard shows active key info
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-key-management/04-05-SUMMARY.md`
</output>
