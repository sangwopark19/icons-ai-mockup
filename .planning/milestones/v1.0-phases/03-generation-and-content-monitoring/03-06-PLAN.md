---
phase: 03-generation-and-content-monitoring
plan: 06
type: execute
wave: 3
depends_on:
  - 03-04
  - 03-05
files_modified: []
autonomous: false
requirements:
  - GEN-01
  - GEN-02
  - GEN-03
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "All Phase 3 features visually work end-to-end"
    - "Full test suite passes"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 3 features.

Purpose: Human confirms that generation monitoring and content management UI works correctly before marking phase complete.
Output: Verified phase ready for `/gsd:verify-work`.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-generation-and-content-monitoring/03-CONTEXT.md
@.planning/phases/03-generation-and-content-monitoring/03-04-SUMMARY.md
@.planning/phases/03-generation-and-content-monitoring/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and typecheck</name>
  <files></files>
  <action>
    Run the full automated verification:
    1. `cd apps/api && npx vitest run` — all unit tests pass
    2. `npx turbo typecheck` — no TypeScript errors
    3. Report results
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/icons-ai-mockup/apps/api && npx vitest run && cd /Users/sangwopark19/icons/icons-ai-mockup && npx turbo typecheck</automated>
  </verify>
  <done>All tests pass, TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of Phase 3 features</name>
  <files></files>
  <action>
    Present the verification checklist to the user for manual testing of all Phase 3 features:
    - Generation monitoring: table, status tabs, search, detail modal, retry
    - Content management: image grid, filters, lightbox, individual delete, bulk delete
  </action>
  <verify>User confirms all features work correctly</verify>
  <done>User types "approved" confirming Phase 3 is complete</done>
  <what-built>
    Phase 3 generation monitoring and content management features:
    1. Generation job table with status tabs (all/pending/processing/completed/failed) and count badges
    2. Email search for generation jobs
    3. Failed job detail modal with error info and retry button
    4. Image content grid with filters (email, date range, project)
    5. Image lightbox with metadata and individual delete
    6. Bulk delete with count confirmation
  </what-built>
  <how-to-verify>
    1. Start the dev servers: `pnpm dev` (or appropriate command)
    2. Navigate to /admin/content as an admin user
    3. Verify "Generation" tab:
       - Table shows generation jobs with correct columns
       - Status tabs display count badges
       - Click a status tab to filter
       - Search by email
       - If failed jobs exist: click detail button to see error detail modal
       - Click retry on a failed job — status should change to pending
    4. Switch to "Content" tab:
       - Image grid displays with thumbnails
       - Apply filters (email search, date range, project dropdown)
       - Click an image to open lightbox
       - Verify metadata shows (size, resolution, date, user, project)
       - Test individual delete from lightbox (with confirmation)
       - Test bulk delete (apply filter, click bulk delete, verify count in confirmation)
    5. Verify pagination works on both tabs
    6. Wait 30+ seconds to verify auto-polling refreshes data
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full test suite green
- TypeScript clean
- Human verified all features visually
</verification>

<success_criteria>
- All automated tests pass
- Human approves visual and functional behavior
- Phase 3 ready for `/gsd:verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/03-generation-and-content-monitoring/03-06-SUMMARY.md`
</output>
