---
phase: 01-auth-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/web/src/stores/auth.store.ts
  - apps/web/src/components/admin/admin-guard.tsx
  - apps/web/src/components/admin/admin-sidebar.tsx
  - apps/web/src/app/admin/layout.tsx
  - apps/web/src/app/admin/page.tsx
  - apps/web/src/app/admin/dashboard/page.tsx
  - apps/web/src/app/admin/users/page.tsx
  - apps/web/src/app/admin/content/page.tsx
  - apps/web/src/app/admin/api-keys/page.tsx
autonomous: false
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Non-admin user navigating to /admin is silently redirected to /"
    - "Non-authenticated user navigating to /admin is silently redirected to /"
    - "No admin UI flashes before redirect for non-admin users"
    - "Admin user sees left sidebar with 4 navigation items"
    - "Sidebar shows admin name and email at top"
    - "Sidebar has 'go to main' link at bottom"
    - "Phase 2-4 menu items link to stub pages that render without error"
    - "Sidebar is responsive: always visible on desktop, hamburger menu on mobile"
  artifacts:
    - path: "apps/web/src/stores/auth.store.ts"
      provides: "User interface with optional role field"
      contains: "role?: string"
    - path: "apps/web/src/components/admin/admin-guard.tsx"
      provides: "Client-side admin route guard"
      contains: "use client"
    - path: "apps/web/src/components/admin/admin-sidebar.tsx"
      provides: "Admin sidebar navigation with 4 menu items"
      contains: "대시보드"
    - path: "apps/web/src/app/admin/layout.tsx"
      provides: "Admin shell wrapping AdminGuard + AdminSidebar + content area"
      contains: "AdminGuard"
    - path: "apps/web/src/app/admin/dashboard/page.tsx"
      provides: "Dashboard stub page"
    - path: "apps/web/src/app/admin/users/page.tsx"
      provides: "Users stub page"
    - path: "apps/web/src/app/admin/content/page.tsx"
      provides: "Content stub page"
    - path: "apps/web/src/app/admin/api-keys/page.tsx"
      provides: "API Keys stub page"
  key_links:
    - from: "apps/web/src/app/admin/layout.tsx"
      to: "apps/web/src/components/admin/admin-guard.tsx"
      via: "AdminGuard wraps all admin children"
      pattern: "<AdminGuard>"
    - from: "apps/web/src/components/admin/admin-guard.tsx"
      to: "apps/web/src/stores/auth.store.ts"
      via: "useAuthStore reads user.role"
      pattern: "useAuthStore.*role"
    - from: "apps/web/src/app/admin/layout.tsx"
      to: "apps/web/src/components/admin/admin-sidebar.tsx"
      via: "AdminSidebar rendered in layout flex container"
      pattern: "<AdminSidebar"
---

<objective>
Frontend admin shell: Zustand role field, AdminGuard route protection, AdminSidebar navigation, admin layout, and stub pages for all 4 sections.

Purpose: Non-admin users are blocked from seeing any admin UI (AUTH-04), and admin users get the full navigation shell (AUTH-05) that later phases populate with content.
Output: Complete /admin route tree with guard, sidebar, and placeholder pages.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-foundation/01-CONTEXT.md
@.planning/phases/01-auth-foundation/01-RESEARCH.md
@.planning/phases/01-auth-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Zustand auth store to be modified (from Plan 01, this store is NOT modified by Plan 01) -->

From apps/web/src/stores/auth.store.ts:
```typescript
interface User {
  id: string;
  email: string;
  name: string;
  // role?: string  — to be added by this plan
}

interface AuthState {
  user: User | null;
  accessToken: string | null;
  refreshToken: string | null;
  isAuthenticated: boolean;
  isLoading: boolean;  // starts true, set to false after Zustand rehydration
}

export const useAuthStore = create<AuthState & AuthActions>()(persist(...));
```

<!-- Existing CSS variable usage pattern from the codebase -->
From apps/web/src/app/(auth)/layout.tsx:
```typescript
<div className="flex min-h-screen items-center justify-center bg-[var(--bg-primary)] px-4">
  <div className="rounded-xl border border-[var(--border-default)] bg-[var(--bg-secondary)] p-8">
```

<!-- Available CSS variables (from globals.css @theme) -->
Available: --bg-primary, --bg-secondary, --border-default, --text-primary, --text-secondary

<!-- lucide-react icons available (confirmed in package.json) -->
Icons: LayoutDashboard, Users, ImageIcon, Key, Menu, X, ChevronLeft (from lucide-react)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth store update + AdminGuard + AdminSidebar components</name>
  <files>apps/web/src/stores/auth.store.ts, apps/web/src/components/admin/admin-guard.tsx, apps/web/src/components/admin/admin-sidebar.tsx</files>
  <action>
1. Modify `apps/web/src/stores/auth.store.ts`:
   - Add `role?: string` to the User interface (after `name`). This is the ONLY change — do not touch AuthState, AuthActions, or the store implementation. The `role` field is optional so existing localStorage data (without role) still rehydrates correctly.

2. Create `apps/web/src/components/admin/admin-guard.tsx`:
   - `'use client'` directive at top
   - Import `useEffect` from react, `useRouter` from `next/navigation`, `useAuthStore` from `@/stores/auth.store`
   - Component: `AdminGuard({ children }: { children: React.ReactNode })`
   - Read `user`, `isLoading`, `isAuthenticated` from `useAuthStore()`
   - useEffect: if `!isLoading` and (`!isAuthenticated` or `user?.role !== 'admin'`) -> `router.replace('/')` (silent redirect to home, NO toast, NO alert)
   - Render: return `null` while `isLoading || !isAuthenticated || user?.role !== 'admin'` — prevents admin UI flash
   - Return `<>{children}</>` only when confirmed admin

3. Create `apps/web/src/components/admin/admin-sidebar.tsx`:
   - `'use client'` directive (needs useState for mobile menu toggle, usePathname for active state)
   - Import icons from lucide-react: `LayoutDashboard`, `Users`, `ImageIcon`, `Key`, `Menu`, `X`
   - Import `usePathname` from `next/navigation`, `Link` from `next/link`, `useAuthStore`
   - Nav items array:
     ```
     { href: '/admin/dashboard', label: '대시보드', icon: LayoutDashboard }
     { href: '/admin/users', label: '사용자 관리', icon: Users }
     { href: '/admin/content', label: '생성/콘텐츠', icon: ImageIcon }
     { href: '/admin/api-keys', label: 'API 키', icon: Key }
     ```
   - Desktop: Always-visible sidebar, width w-64, `bg-[var(--bg-secondary)]` background, `border-r border-[var(--border-default)]`
   - Mobile: Hidden by default (`hidden md:flex` for desktop, hamburger button visible on mobile via `md:hidden`)
   - Mobile menu: Full-screen overlay with same sidebar content, toggled by hamburger `Menu`/`X` icon
   - Active state: Current pathname starts with item.href -> highlight with different bg color
   - Top section: Admin user name + email from `useAuthStore().user` — `text-[var(--text-primary)]` for name, `text-[var(--text-secondary)]` for email
   - Bottom section: Link to `/` with text "메인으로" (back to main app)
   - Sidebar layout: flex flex-col h-screen, nav items in middle (flex-1), user info at top, "메인으로" at bottom
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/icons-ai-mockup && pnpm --filter web tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>auth.store.ts has role?: string on User. AdminGuard blocks non-admin with null render + redirect. AdminSidebar shows 4 nav items with responsive mobile hamburger menu. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Admin layout + stub pages</name>
  <files>apps/web/src/app/admin/layout.tsx, apps/web/src/app/admin/page.tsx, apps/web/src/app/admin/dashboard/page.tsx, apps/web/src/app/admin/users/page.tsx, apps/web/src/app/admin/content/page.tsx, apps/web/src/app/admin/api-keys/page.tsx</files>
  <action>
1. Create `apps/web/src/app/admin/layout.tsx`:
   - Import `AdminGuard` from `@/components/admin/admin-guard`
   - Import `AdminSidebar` from `@/components/admin/admin-sidebar`
   - This is a SERVER component wrapper (no 'use client') — the guard and sidebar are client components
   - Structure:
     ```tsx
     <AdminGuard>
       <div className="flex min-h-screen bg-[var(--bg-primary)]">
         <AdminSidebar />
         <main className="flex-1 overflow-auto p-6">
           {children}
         </main>
       </div>
     </AdminGuard>
     ```

2. Create `apps/web/src/app/admin/page.tsx`:
   - `redirect('/admin/dashboard')` using `next/navigation` redirect function
   - This is a server component — use the server-side redirect

3. Create stub pages — each is a minimal server component with just a title heading:
   - `apps/web/src/app/admin/dashboard/page.tsx`: `<h1 className="text-2xl font-bold text-[var(--text-primary)]">대시보드</h1>`
   - `apps/web/src/app/admin/users/page.tsx`: `<h1 className="text-2xl font-bold text-[var(--text-primary)]">사용자 관리</h1>`
   - `apps/web/src/app/admin/content/page.tsx`: `<h1 className="text-2xl font-bold text-[var(--text-primary)]">생성/콘텐츠</h1>`
   - `apps/web/src/app/admin/api-keys/page.tsx`: `<h1 className="text-2xl font-bold text-[var(--text-primary)]">API 키</h1>`
   - Each stub page exports a default function component. NO "준비 중" messages — just the page title per user decision.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/icons-ai-mockup && pnpm --filter web tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Admin layout wraps guard + sidebar + content area. /admin redirects to /admin/dashboard. All 4 stub pages render a title heading. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete admin auth flow</name>
  <files>none</files>
  <action>
Human verification of the complete admin auth flow across both plans (01-01 backend + 01-02 frontend). Start dev servers and run through the verification steps below.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/icons-ai-mockup && pnpm --filter api test && pnpm --filter web tsc --noEmit</automated>
  </verify>
  <done>User approves: non-admin redirect works silently, admin sees sidebar with 4 nav items, mobile responsive, API returns correct 401/403.</done>
</task>

</tasks>

<checkpoint_details task="Task 3">
  <what-built>Complete admin auth flow: backend (role migration, JWT role, requireAdmin middleware) + frontend (admin guard, sidebar layout with 4 nav items, stub pages). Non-admin users are silently redirected away from /admin.</what-built>
  <how-to-verify>
    1. Start dev servers: `pnpm dev` (or however the project starts both API and web)
    2. **Test non-admin redirect:**
       - Log in as a regular user (non-admin account)
       - Navigate to /admin in the browser
       - Verify: silently redirected to / with no flash of admin UI and no toast/alert
    3. **Test unauthenticated redirect:**
       - Log out
       - Navigate to /admin
       - Verify: silently redirected to /
    4. **Test admin access:**
       - Log in as admin (admin@example.com / admin1234! — created by seed)
       - Navigate to /admin
       - Verify: redirected to /admin/dashboard
       - Verify: left sidebar visible with 4 items (대시보드, 사용자 관리, 생성/콘텐츠, API 키)
       - Verify: admin name + email shown at top of sidebar
       - Verify: "메인으로" link at bottom of sidebar
    5. **Test sidebar navigation:**
       - Click each nav item — verify it navigates and shows the page title
       - Active item should be visually highlighted
    6. **Test mobile responsive:**
       - Resize browser to mobile width
       - Verify: sidebar hidden, hamburger menu icon visible
       - Click hamburger — verify sidebar opens as overlay
    7. **Test API security:**
       - In browser devtools console: `fetch('/api/admin/health', { headers: { Authorization: 'Bearer INVALID' }}).then(r => r.json()).then(console.log)`
       - Verify: returns 401
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</checkpoint_details>

<verification>
- `pnpm --filter web tsc --noEmit` — no type errors in web app
- `pnpm --filter api test` — all backend tests pass
- Non-admin redirect works without UI flash
- Admin sees sidebar with all 4 nav items
- API endpoint security verified (401/403)
</verification>

<success_criteria>
- Non-admin navigating to /admin is silently redirected to / (no flash, no toast)
- Admin user sees /admin/dashboard with sidebar containing 4 menu items
- Sidebar shows admin name + email at top, "메인으로" link at bottom
- Sidebar is responsive (desktop: always visible, mobile: hamburger toggle)
- All stub pages (/admin/dashboard, /admin/users, /admin/content, /admin/api-keys) render with title
- /admin root redirects to /admin/dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-foundation/01-02-SUMMARY.md`
</output>
