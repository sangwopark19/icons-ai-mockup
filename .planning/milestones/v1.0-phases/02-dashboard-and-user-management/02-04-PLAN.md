---
phase: 02-dashboard-and-user-management
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - USER-01
  - USER-02
  - USER-03
  - USER-04
  - USER-05

must_haves:
  truths:
    - "All dashboard KPI cards render with real or placeholder data"
    - "Recharts bar chart is visible and not blank"
    - "User table loads with pagination"
    - "Suspend and delete flows show confirmation modals"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 2 implementation: dashboard KPI cards, failure chart, user management table, and account lifecycle actions.

Purpose: Human confirms the UI renders correctly and interactive flows work end-to-end before marking Phase 2 complete.
Output: Verification approval or issue list for fixes.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-and-user-management/02-CONTEXT.md
@.planning/phases/02-dashboard-and-user-management/02-02-SUMMARY.md
@.planning/phases/02-dashboard-and-user-management/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of Phase 2</name>
  <files></files>
  <action>
    Present the verification checklist to the user. Start both apps if not already running (`pnpm dev` in root or start API and web separately). Ensure admin user exists (admin@example.com from Phase 1 seed).
  </action>
  <verify>Human confirms all 22 verification steps pass</verify>
  <done>User types "approved" or provides issue list</done>
  <what-built>
    Complete Phase 2: Admin dashboard with 6 KPI cards (2x3 grid), hourly failure bar chart, and user management page with searchable/filterable table, pagination, and account lifecycle actions (suspend/unsuspend, soft delete, role change).
  </what-built>
  <how-to-verify>
    Prerequisites: Start both apps (`pnpm dev` in root or `cd apps/api && pnpm dev` + `cd apps/web && pnpm dev`). Log in as admin (admin@example.com).

    **Dashboard verification:**
    1. Navigate to /admin/dashboard
    2. Verify 6 KPI cards appear in a 2x3 grid layout
    3. Verify each card shows a number and label (total users, generations, failed jobs, queue depth, storage, API keys)
    4. Verify API Keys card shows "N/A" placeholder
    5. Verify day-over-day delta arrows/percentages appear on applicable cards
    6. Verify skeleton loading briefly appears on page load (may be fast on localhost)
    7. Scroll down -- verify the bar chart section is visible with title "시간대별 실패 건수 (24시간)"
    8. Verify the chart is NOT blank (Recharts React 19 issue -- if blank, report)
    9. Wait 30+ seconds -- verify the KPI numbers update (check browser DevTools Network tab for polling requests)

    **User management verification:**
    10. Navigate to /admin/users
    11. Verify user table displays with columns: email, name, role, status, created, last login, action
    12. Verify pagination buttons appear at the bottom
    13. Type an email in the search bar -- verify results filter (with slight debounce delay)
    14. Select a role or status filter -- verify results update
    15. Click the action menu (three dots) on a non-admin user row
    16. Click "계정 정지" -- verify confirmation modal appears with user email
    17. Confirm -- verify the user's status badge changes to "suspended"
    18. Click action menu again -- verify it now shows "정지 해제" instead of "계정 정지"
    19. Click "정지 해제" -- verify status returns to "active" (no modal)
    20. Click action menu -- click "역할 변경" -- verify role toggles without modal
    21. Click action menu -- click "계정 삭제" -- verify danger confirmation modal appears
    22. Verify your own row (admin@example.com) has no functional action menu or disabled actions
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Human visual and functional verification of all Phase 2 features.
</verification>

<success_criteria>
- All 22 verification steps pass
- No blank chart (Recharts renders correctly)
- All interactive flows complete without errors
- Self-action prevention works
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-and-user-management/02-04-SUMMARY.md`
</output>
