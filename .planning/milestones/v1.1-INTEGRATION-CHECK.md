# v1.1 Integration Check: OpenAI GPT Image 2 Dual Provider

Checked: 2026-05-04T00:51:41Z

## Verdict

`gaps_found`

The source wiring for provider-aware generation, OpenAI IP Change, OpenAI Sketch to Real, same-provider regenerate, OpenAI partial edit, style-copy continuation, and admin retry recovery is connected end to end. The integration gate still finds one milestone blocker: the product result/history UI exposes only `v1`/`v2`, while literal requirement `PROV-03` says users can see both provider and model.

The integration checker also ran:

- `pnpm --filter @mockup-ai/api test ...` - PASS, 7 files / 149 tests
- `pnpm --filter @mockup-ai/api type-check` - PASS
- `pnpm --filter @mockup-ai/web type-check` - PASS

## Requirement IDs Covered

`PROV-01`, `PROV-02`, `PROV-03`, `PROV-04`, `OIP-01`, `OIP-02`, `OIP-03`, `OSR-01`, `OSR-02`, `OSR-03`, `OED-01`, `OED-02`, `OED-03`, `OPS-01`, `OPS-02`, `OPS-03`, `OPS-04`

## Cross-Phase Wiring

| Flow | Requirement IDs | Status | Evidence |
|---|---|---|---|
| Provider schema/admin key separation -> worker provider dispatch | `OPS-01`, `OPS-02`, `OPS-03`, `OPS-04` | WIRED | `Provider`, `providerModel`, and OpenAI metadata fields are persisted in `apps/api/prisma/schema.prisma`; admin key APIs require provider in `apps/api/src/routes/admin/api-keys.routes.ts`; worker validates queued provider/model before runtime calls in `apps/api/src/worker.ts`. |
| Project page -> OpenAI IP Change form -> API -> queue -> worker -> OpenAI runtime -> result/history | `PROV-01`, `OIP-01`, `OIP-02`, `OIP-03` | WIRED_WITH_HUMAN_NEEDED | Project links v1/v2 IP Change routes; the OpenAI form posts `provider: openai`, `providerModel: gpt-image-2`, and `outputCount: 2`; worker dispatches to `generateIPChange`. Live OpenAI/browser evidence remains pending. |
| Project page -> OpenAI Sketch form -> API -> queue -> worker -> OpenAI runtime -> transparent post-process -> result/history | `PROV-02`, `OSR-01`, `OSR-02`, `OSR-03` | WIRED_WITH_EXCEPTION | Sketch v2 form posts OpenAI payload and `transparentBackground`; worker calls `generateSketchToReal` and then `removeUniformLightBackground` for transparent requests. Final live transparent PNG evidence remains exception-scoped. |
| Result/history provider/model display | `PROV-03` | BROKEN / BLOCKER | API returns `provider` and `providerModel`, but result/history UI renders only `v1`/`v2` and does not display the model. The literal requirement says users can see provider and model. |
| Same-provider regenerate | `PROV-04` | WIRED | Result page calls `/regenerate`; service copies original `provider`, `providerModel`, saved inputs, prompt, and options before requeue. |
| OpenAI partial edit from existing result page | `OED-01`, `OED-03` | WIRED_WITH_HUMAN_NEEDED | Result page sends `selectedImageId` to `/edit`; edit route branches on persisted provider and calls OpenAI. Live OpenAI edit smoke remains pending. |
| OpenAI style-copy continuation and retry recovery | `OED-02`, `OED-03`, `OPS-03` | WIRED_WITH_HUMAN_NEEDED | Result page routes OpenAI style-copy with `styleRef`, `copyTarget`, and `imageId`; dedicated style-copy page verifies `provider === 'openai'`; admin retry restores `copyTarget` and `selectedImageId`. Live style-copy smoke remains pending. |

## Broken Flows

### BLOCKER: PROV-03 product result/history does not display model

Affected requirement: `PROV-03`

Provider/model data is persisted and returned by API, but the user-facing result and history surfaces only show `v1` or `v2`. If `v1/v2` is the intended product abstraction, `PROV-03` should be reworded. If the original requirement stands, result/history must render at least provider plus model label.

Evidence:

- API includes `provider` and `providerModel`: `apps/api/src/routes/generation.routes.ts`
- Result UI derives `isV2` and renders only `v1`/`v2`: `apps/web/src/app/projects/[id]/generations/[genId]/page.tsx`
- History UI renders only `v1`/`v2`: `apps/web/src/app/projects/[id]/history/page.tsx`

## Warnings And Human-Needed Evidence

- `PROV-01`, `OIP-01`, `OIP-02`, `OIP-03`: Phase 8 live OpenAI IP Change smoke and authenticated browser walkthrough remain `human_needed`.
- `OSR-03`: transparent Sketch output is source/test wired, but final live PNG alpha/composite evidence is still missing under the documented milestone exception.
- `OED-01`, `OED-02`, `OED-03`: live OpenAI partial edit/style-copy/browser checks remain `human_needed`.
- No requirement is completely unwired. The gap is one data-to-display break for `PROV-03` plus documented live-evidence debt.

## Final Verdict

Milestone v1.1 should not be marked fully passed under the literal requirements because `PROV-03` is broken at the UI display layer. All other inspected cross-phase source wiring is connected end to end, with remaining OpenAI/live browser evidence tracked as `human_needed` or exception-scoped tech debt.
