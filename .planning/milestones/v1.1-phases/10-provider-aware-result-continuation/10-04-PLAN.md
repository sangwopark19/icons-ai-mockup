---
phase: 10-provider-aware-result-continuation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/generation.service.ts
  - apps/api/src/services/__tests__/generation.service.test.ts
  - apps/api/src/routes/generation.routes.ts
  - apps/api/src/lib/queue.ts
  - packages/shared/src/types/index.ts
autonomous: true
requirements: [PROV-04, OED-02, OED-03]
must_haves:
  truths:
    - "D-05/D-06/D-07/D-08 - OpenAI regeneration reuses persisted provider/model/inputs/prompt/options, creates a fresh request, returns two candidates, and fails clearly on missing inputs."
    - "D-15/D-20 - OpenAI style-copy supports `ip-change` and `new-product` targets and creates two candidates."
    - "D-21 - style-copy enqueue data carries OpenAI lineage context without Gemini `thoughtSignature`."
  artifacts:
    - path: "apps/api/src/services/generation.service.ts"
      provides: "Provider-pinned regenerate and style-copy creation"
    - path: "apps/api/src/routes/generation.routes.ts"
      provides: "Validated copy-style route payload"
    - path: "apps/api/src/lib/queue.ts"
      provides: "Continuation job fields for worker execution"
  key_links:
    - from: "generationService.regenerate"
      to: "generationService.create"
      via: "persisted provider/model/input replay"
      pattern: "provider: original.provider"
    - from: "generationService.copyStyle"
      to: "addGenerationJob"
      via: "styleReferenceId, selectedImageId, copyTarget"
      pattern: "copyTarget"
---

<objective>
Make regeneration and style-copy creation provider-pinned before worker execution.

Purpose: The API/service layer must create continuation generations from persisted source records, not from badges or client provider state. This plan intentionally has no dependency on Plan 10-02 because it does not call OpenAI runtime helpers; Plan 10-05 depends on both contracts.
Output: Tested service and route contracts for OpenAI same-condition regeneration and style-copy enqueue data.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-provider-aware-result-continuation/10-CONTEXT.md
@.planning/phases/10-provider-aware-result-continuation/10-RESEARCH.md
@.planning/phases/10-provider-aware-result-continuation/10-PATTERNS.md
@.planning/phases/10-provider-aware-result-continuation/10-VALIDATION.md
@.codex/skills/mockup-openai-dual-provider/SKILL.md
@.codex/skills/mockup-openai-workflows/references/workflow-matrix.md
@.codex/skills/mockup-precision-edit/references/gpt-image-2-notes.md

<interfaces>
Current queue contract:
```ts
export interface GenerationJobData {
  generationId: string;
  provider: 'gemini' | 'openai';
  providerModel: string;
  styleReferenceId?: string;
  sourceImagePath?: string;
  characterImagePath?: string;
  textureImagePath?: string;
  options: { outputCount: number; quality?: 'low' | 'medium' | 'high' };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace OpenAI regeneration rejection with provider-pinned replay</name>
  <files>apps/api/src/services/generation.service.ts, apps/api/src/services/__tests__/generation.service.test.ts</files>
  <read_first>
    - apps/api/src/services/generation.service.ts
    - apps/api/src/services/__tests__/generation.service.test.ts
    - apps/api/src/lib/queue.ts
    - .planning/phases/10-provider-aware-result-continuation/10-CONTEXT.md
    - .planning/phases/10-provider-aware-result-continuation/10-VALIDATION.md
  </read_first>
  <behavior>
    - D-05: regenerated OpenAI record uses `provider: 'openai'`, `providerModel: 'gpt-image-2'`, original input paths, prompt, and options.
    - D-06: no selected output image is used as a seed/reference for regeneration.
    - D-07: regenerated OpenAI job has `options.outputCount === 2`.
    - D-08: missing source inputs throw `재생성 입력값이 불완전합니다` before `prisma.generation.create` or `addGenerationJob`.
  </behavior>
  <action>
    In `generation.service.test.ts`, replace the current test named `rejects OpenAI v2 regeneration before enqueueing a failing worker job` with positive and negative tests:
    - `replays OpenAI regeneration with persisted provider model inputs prompt and options`
    - `fails OpenAI regeneration clearly when stored source inputs are missing`

    In `generation.service.ts`, remove the `if (original.provider === 'openai') throw ...` guard. Keep `validateRegenerationInputs()` mandatory for all providers. Preserve the existing replay object but force `options.outputCount` to `(options.outputCount as number | undefined) ?? 2`, and add a guard after normalization that OpenAI regeneration cannot use `selectedImageId`, `sourceImageId`, or any generated output path as a new `sourceImagePath`.

    The positive test must assert `prisma.generation.create.mock.calls[0][0].data.provider === 'openai'`, `providerModel === 'gpt-image-2'`, promptData contains original `sourceImagePath`, `characterImagePath` or `textureImagePath`, and `addGenerationJob.mock.calls[0][0].provider === 'openai'` with `options.outputCount === 2`.

    Addresses review concern: Plan 10-04 does not use OpenAI service helpers and therefore remains parallel with Plan 10-02; keep `depends_on: []` unless this task adds a direct import from `openai-image.service.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api test -- src/services/__tests__/generation.service.test.ts</automated>
    <automated>rg -n "replays OpenAI regeneration with persisted provider model inputs prompt and options|fails OpenAI regeneration clearly when stored source inputs are missing|provider: original\\.provider|providerModel: original\\.providerModel" apps/api/src/services/generation.service.ts apps/api/src/services/__tests__/generation.service.test.ts</automated>
    <automated>! rg -n "OpenAI .*동일 조건 재생성을 지원하지 않습니다" apps/api/src/services/generation.service.ts apps/api/src/services/__tests__/generation.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The old OpenAI regeneration rejection string is absent.
    - Tests prove OpenAI regeneration enqueues with `provider: 'openai'`, `providerModel: 'gpt-image-2'`, and `outputCount: 2`.
    - Tests prove missing stored inputs fail before DB creation and queue enqueue.
    - `rg -n "openai-image.service" apps/api/src/services/generation.service.ts apps/api/src/routes/generation.routes.ts apps/api/src/lib/queue.ts packages/shared/src/types/index.ts` returns no matches; if it finds a match, this plan must be moved behind Plan 10-02.
  </acceptance_criteria>
  <done>OpenAI same-condition regeneration creates provider-pinned two-candidate jobs and fails clearly on invalid stored inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add style-copy route and queue contract fields</name>
  <files>apps/api/src/services/generation.service.ts, apps/api/src/services/__tests__/generation.service.test.ts, apps/api/src/routes/generation.routes.ts, apps/api/src/lib/queue.ts, packages/shared/src/types/index.ts</files>
  <read_first>
    - apps/api/src/services/generation.service.ts
    - apps/api/src/services/__tests__/generation.service.test.ts
    - apps/api/src/routes/generation.routes.ts
    - apps/api/src/lib/queue.ts
    - packages/shared/src/types/index.ts
    - .planning/phases/10-provider-aware-result-continuation/10-CONTEXT.md
  </read_first>
  <behavior>
    - D-15: copy-style accepts `copyTarget: 'ip-change' | 'new-product'`.
    - D-17: route accepts `selectedImageId` so the approved output can be used as the style reference.
    - D-20: created OpenAI style-copy generation uses `options.outputCount === 2`.
    - D-21: queue data carries OpenAI linkage context and does not require `thoughtSignature`.
  </behavior>
  <action>
    Extend `GenerationJobData` in `apps/api/src/lib/queue.ts` with:
    `copyTarget?: 'ip-change' | 'new-product';`
    `selectedImageId?: string;`

    Extend the local `CreateGenerationInput` in `generation.service.ts` with the same optional fields and `providerTrace?: Record<string, unknown>`. Persist `copyTarget` under `promptData.copyTarget`, `selectedImageId` under `promptData.selectedImageId`, and pass both fields to `addGenerationJob`. When creating an OpenAI style-copy generation, write `providerTrace` with these exact mirror keys: `workflow: 'style_copy'`, `copyTarget`, `styleReferenceId: original.id`, and `styleSourceImageId: input.selectedImageId`. Do not add or modify Prisma schema; use the existing JSON `providerTrace` field.

    Extend `CopyStyleSchema` in `generation.routes.ts` to:
    `copyTarget: z.enum(['ip-change', 'new-product']).default('ip-change')`,
    `selectedImageId: z.string().uuid().optional()`,
    `userInstructions: z.string().max(2000).optional()`.
    Keep the existing requirement that either `characterImagePath` or `sourceImagePath` is supplied.

    Update `generationService.copyStyle(userId, generationId, input)` input type to `{ characterImagePath?: string; sourceImagePath?: string; copyTarget?: 'ip-change' | 'new-product'; selectedImageId?: string; userInstructions?: string }`. Remove the OpenAI style-copy rejection. When `original.provider === 'openai'`, require `input.copyTarget`, require `input.selectedImageId`, verify that `input.selectedImageId` belongs to `original.images`, set `provider` and `providerModel` from `original`, set `styleReferenceId: original.id`, set `options.outputCount: 2`, and set `options.userInstructions` to `input.userInstructions?.trim()` or the original option. For `copyTarget === 'ip-change'`, require `characterImagePath`; for `copyTarget === 'new-product'`, require `sourceImagePath`.

    Preserve existing Gemini behavior: Gemini can still use existing `characterImagePath`/`sourceImagePath` copying and `styleReferenceId` without requiring OpenAI linkage. If `original.provider === 'gemini'` and `input.selectedImageId` is supplied, reject with `v2 스타일 복사는 v2 기준 결과에서만 시작할 수 있습니다`; this blocks a direct URL or forged request from mixing OpenAI-only selected-image style-copy state into a Gemini source record.

    Add tests:
    - `creates OpenAI style copy for ip-change with selected image id and two candidates`
    - `creates OpenAI style copy for new-product with selected image id and two candidates`
    - `rejects OpenAI style copy when selected image does not belong to the source generation`
    - `rejects Gemini source generation when selectedImageId indicates v2 style copy`
    - `mirrors OpenAI style copy copyTarget styleReferenceId and styleSourceImageId in providerTrace`
    - `passes copyTarget selectedImageId and userInstructions through the copy-style route`
  </action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api test -- src/services/__tests__/generation.service.test.ts src/routes/__tests__/generation.routes.test.ts</automated>
    <automated>pnpm --filter @mockup-ai/api type-check</automated>
    <automated>rg -n "copyTarget|selectedImageId|styleSourceImageId|styleReferenceId|creates OpenAI style copy for ip-change|creates OpenAI style copy for new-product|rejects Gemini source generation when selectedImageId indicates v2 style copy|passes copyTarget selectedImageId and userInstructions through the copy-style route" apps/api/src/services/generation.service.ts apps/api/src/services/__tests__/generation.service.test.ts apps/api/src/routes/generation.routes.ts apps/api/src/routes/__tests__/generation.routes.test.ts apps/api/src/lib/queue.ts packages/shared/src/types/index.ts</automated>
    <automated>! rg -n "OpenAI .*스타일 복사를 지원하지 않습니다" apps/api/src/services/generation.service.ts apps/api/src/services/__tests__/generation.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GenerationJobData` contains `copyTarget?: 'ip-change' | 'new-product'` and `selectedImageId?: string`.
    - `CopyStyleSchema` validates `copyTarget`, `selectedImageId`, and `userInstructions`.
    - Old OpenAI style-copy rejection string is absent.
    - Tests prove OpenAI style-copy enqueue data contains `styleReferenceId`, `copyTarget`, `selectedImageId`, provider/model, and `outputCount: 2`.
    - Tests prove OpenAI style-copy creation writes `providerTrace.copyTarget`, `providerTrace.styleReferenceId`, and `providerTrace.styleSourceImageId`.
    - Tests prove a Gemini source generation with `selectedImageId` is rejected with `v2 스타일 복사는 v2 기준 결과에서만 시작할 수 있습니다`.
  </acceptance_criteria>
  <done>OpenAI style-copy jobs are created with provider-pinned lineage data and target-specific payloads.</done>
</task>

</tasks>

<must_haves>
- PROV-04 is covered by provider-pinned regeneration replay.
- OED-02 is covered at creation/enqueue time for both style-copy targets.
- OED-03 is supported because OpenAI copy-style no longer blocks before worker execution and carries OpenAI-specific lineage data instead of Gemini state.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API route -> service | Authenticated user input selects copy target, selected image ID, and target upload path. |
| Service -> queue | Persisted generation provider/model and selected-image lineage become worker job data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | regeneration | mitigate | Reuse persisted promptData/options and validate normalized original vs replay before enqueue. |
| T-10-04-02 | Information Disclosure | style-copy selected image | mitigate | Verify `selectedImageId` belongs to `original.images` from `getById(userId, generationId)`. |
| T-10-04-03 | Elevation of Privilege | provider confusion | mitigate | Copy `original.provider` and `original.providerModel` into new generation and queue payload. |
| T-10-04-04 | Tampering | copy target | mitigate | Validate `copyTarget` with Zod enum and require the correct target asset path for each value. |
| T-10-04-05 | Repudiation | OpenAI style-copy metadata | mitigate | Mirror `copyTarget`, `styleReferenceId`, and `styleSourceImageId` into existing `providerTrace` for support/debugging without schema changes. |
</threat_model>

<verification>
Run targeted generation service and generation route tests, then API type-check.
</verification>

<success_criteria>
- OpenAI regeneration no longer rejects supported v2 records.
- OpenAI copy-style accepts both targets, validates selected-image ownership, and enqueues two-candidate jobs.
- No Prisma schema files are modified; no `npx prisma db push` task is required.
</success_criteria>

<output>
After completion, create `.planning/phases/10-provider-aware-result-continuation/10-04-SUMMARY.md`.
</output>
