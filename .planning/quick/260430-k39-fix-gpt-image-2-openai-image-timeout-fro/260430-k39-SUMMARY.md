---
status: complete
quick_task: 260430-k39
plan: 260430-k39-fix-gpt-image-2-openai-image-timeout
completed_at: 2026-04-30T05:37:41Z
files_modified:
  - apps/api/src/services/openai-image.service.ts
  - apps/api/src/services/__tests__/openai-image.service.test.ts
  - apps/web/src/app/projects/[id]/ip-change/openai/page.tsx
  - apps/web/src/app/projects/[id]/sketch-to-real/openai/page.tsx
---

# Quick Task 260430-k39 Summary

OpenAI GPT Image 2 SDK timeout을 `OPENAI_IMAGE_TIMEOUT_MS`로 설정 가능하게 만들고 기본값을 `600_000`ms로 올렸습니다. SDK retry budget은 `maxRetries: 0`으로 유지했고, OpenAI IP Change와 Sketch-to-Real 품질 모드에 예상 대기 시간을 표시했습니다.

## Changes

- `apps/api/src/services/openai-image.service.ts`
  - `OPENAI_IMAGE_TIMEOUT_MS` resolver 추가
  - empty, non-numeric, zero, negative env 값은 `600_000`으로 fallback
  - 다섯 개 OpenAI client 생성 지점의 hardcoded `60_000` timeout 제거
- `apps/api/src/services/__tests__/openai-image.service.test.ts`
  - 기본 timeout `600_000` assertion 반영
  - env override `900000` regression test 추가
  - invalid env fallback regression test 추가
- `apps/web/src/app/projects/[id]/ip-change/openai/page.tsx`
  - 빠른/균형/퀄리티 모드에 예상 대기 문구 표시
- `apps/web/src/app/projects/[id]/sketch-to-real/openai/page.tsx`
  - IP Change와 동일한 예상 대기 문구 표시

## Commands Run

| Command | Status | Notes |
| --- | --- | --- |
| `pnpm --filter @mockup-ai/api test -- src/services/__tests__/openai-image.service.test.ts src/lib/__tests__/queue.test.ts` | failed initially | `node_modules` missing, `vitest: command not found` |
| `pnpm install --frozen-lockfile` | passed | Installed workspace dependencies from lockfile |
| `pnpm --filter @mockup-ai/api test -- src/services/__tests__/openai-image.service.test.ts src/lib/__tests__/queue.test.ts` | passed | 2 test files, 27 tests passed |
| `pnpm --filter @mockup-ai/api type-check` | failed initially | Prisma client types missing after install |
| `pnpm --filter @mockup-ai/api db:generate` | passed | Generated Prisma client |
| `pnpm --filter @mockup-ai/api type-check` | passed | `tsc --noEmit` clean |
| `pnpm --filter @mockup-ai/web type-check` | passed | `tsc --noEmit` clean |
| `pnpm --filter @mockup-ai/web lint` | passed with warnings | 0 errors, 11 pre-existing warnings in unrelated files |
| `rg -n "timeout:\\s*60_000" apps/api/src/services/openai-image.service.ts` | passed | No matches; command exited 1 as expected |
| `rg -n "예상 대기 1-4분|예상 대기 4-10분|예상 대기 8-15분" 'apps/web/src/app/projects/[id]/ip-change/openai/page.tsx' 'apps/web/src/app/projects/[id]/sketch-to-real/openai/page.tsx'` | passed | Six matches across both pages |
| `git diff --check -- apps/api/src/services/openai-image.service.ts apps/api/src/services/__tests__/openai-image.service.test.ts 'apps/web/src/app/projects/[id]/ip-change/openai/page.tsx' 'apps/web/src/app/projects/[id]/sketch-to-real/openai/page.tsx'` | passed | No whitespace errors |

## Deviations

- Dependency install was required because this hotfix worktree had no `node_modules`.
- Prisma client generation was required before API type-check could pass.
- No commits were created, per orchestrator constraint.

## Remaining Notes

- `pnpm --filter @mockup-ai/web lint` exits successfully but reports 11 warnings in unrelated existing files.
- BullMQ OpenAI one-attempt behavior was not modified and remains covered by `src/lib/__tests__/queue.test.ts`.

## Self-Check: PASSED

- Summary file created at the requested path.
- Only the four scoped source/test files and this summary were edited.
- Verification commands completed with the statuses recorded above.
