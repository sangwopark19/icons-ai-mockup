---
phase: 03-generation-and-content-monitoring
plan: 05
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-03
files_modified:
  - apps/web/src/app/admin/content/page.tsx
  - apps/web/src/components/admin/content-grid.tsx
  - apps/web/src/components/admin/image-lightbox.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "Admin can browse generated images in a grid layout with thumbnails"
    - "Admin can filter images by user email, date range, and project"
    - "Admin can click an image to see lightbox with full-size preview and metadata"
    - "Admin can delete individual image from lightbox (with confirmation)"
    - "Admin can bulk-delete all images matching current filter (with count confirmation)"
    - "Grid auto-refreshes after any delete operation"
  artifacts:
    - path: "apps/web/src/components/admin/content-grid.tsx"
      provides: "Image grid with filter bar, pagination, bulk delete"
      contains: "ContentGrid"
    - path: "apps/web/src/components/admin/image-lightbox.tsx"
      provides: "Full-size image preview with metadata and delete button"
      contains: "ImageLightbox"
  key_links:
    - from: "apps/web/src/components/admin/content-grid.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "adminApi.listImages, adminApi.countImages, adminApi.bulkDeleteImages"
      pattern: "adminApi\\.(listImages|countImages|bulkDeleteImages)"
    - from: "apps/web/src/components/admin/image-lightbox.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "adminApi.deleteImage"
      pattern: "adminApi\\.deleteImage"
---

<objective>
Build the content browsing and deletion frontend: image grid with filters, lightbox preview, individual delete, and bulk delete with count confirmation.

Purpose: Frontend for CONT-01 (browse/search), CONT-02 (individual delete), CONT-03 (user history via filter), CONT-04 (bulk delete).
Output: Working "Content" tab with full image management UI.
</objective>

<execution_context>
@/Users/sangwopark19/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-generation-and-content-monitoring/03-CONTEXT.md
@.planning/phases/03-generation-and-content-monitoring/03-RESEARCH.md
@.planning/phases/03-generation-and-content-monitoring/03-03-SUMMARY.md

@apps/web/src/app/admin/content/page.tsx
@apps/web/src/lib/api.ts
@apps/web/src/components/admin/ConfirmDialog.tsx
@apps/web/src/components/admin/UserTable.tsx
@apps/api/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContentGrid component with filter bar, image grid, pagination, and bulk delete</name>
  <files>apps/web/src/components/admin/content-grid.tsx, apps/web/src/app/admin/content/page.tsx</files>
  <action>
    1. **Create `content-grid.tsx`** as a `'use client'` component:
       - `useAuthStore` for `accessToken`
       - **State**: `images` (AdminImage[]), `pagination`, `loading`, `page`, filter states: `emailSearch`, `startDate`, `endDate`, `projectId`, `projects` (for dropdown), `selectedImage` (for lightbox), `bulkDeleteCount` (number | null), `showBulkDeleteConfirm`

       - **Filter bar** (single row, per CONTEXT.md decision):
         - User email search input (left)
         - Date range: two native `<input type="date">` inputs (start/end) — per RESEARCH.md recommendation for admin tool
         - Project dropdown: `<select>` populated by fetching projects with images. Use a separate fetch on mount: `adminApi.listProjects` or a simple GET to a projects endpoint. If no project endpoint exists for admin, build a local fetch to `/api/admin/content/projects` (add to content.routes.ts — see note below). Add `<option value="">전체 프로젝트</option>` default.
         - "검색" button to apply filters (resets page to 1)

       - **Image grid**: CSS grid with responsive columns (`grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5`). Each card:
         - Thumbnail image: `<img src="${API_URL}/uploads/${image.thumbnailPath || image.filePath}" />` — verify static file serving path from server.ts before hardcoding. Use `aspect-ratio: 1` + `object-fit: cover`.
         - User email (truncated)
         - Date (formatted)
         - Project name
         - Click handler: sets `selectedImage` to open lightbox

       - **Pagination**: Reuse `getPageNumbers` logic from UserTable. Bottom of grid.

       - **Bulk delete button**: "필터 조건 일괄 삭제" button below filter bar (only visible when at least one filter is active). On click:
         1. Call `adminApi.countImages(token, { email, projectId, startDate, endDate })`
         2. Set `bulkDeleteCount` and `showBulkDeleteConfirm = true`
         3. Show confirmation modal: "필터 조건에 매칭되는 {count}건의 이미지를 삭제합니다" per CONTEXT.md
         4. On confirm: call `adminApi.bulkDeleteImages(token, params)`, show success toast, refresh grid
         5. Use `ConfirmDialog` component pattern for the confirmation modal (danger variant)

       - **Empty state**: "이미지가 없습니다" when no results

       - **30-second polling**: Same useEffect+setInterval pattern. Re-fetch when filters or page change.

    2. **Update `content/page.tsx`**: Replace the placeholder `<div>콘텐츠 (Plan 05)</div>` with `<ContentGrid />` import and render.

    3. **NOTE on project list endpoint**: If no admin endpoint exists to list projects, add a `GET /projects` route to `content.routes.ts` that returns projects with generated images:
       ```typescript
       // In content.routes.ts
       fastify.get('/projects', async (request, reply) => {
         const projects = await prisma.project.findMany({
           where: { generations: { some: { images: { some: {} } } } },
           select: { id: true, name: true },
           take: 200,
         });
         return reply.code(200).send({ success: true, data: projects });
       });
       ```
       And add `listContentProjects` to adminApi in api.ts.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/icons-ai-mockup && npx turbo typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>Content tab shows image grid with filter bar (email, date range, project dropdown). Pagination works. Bulk delete shows count confirmation modal. Grid refreshes after delete.</done>
</task>

<task type="auto">
  <name>Task 2: Create ImageLightbox component with metadata display and individual delete</name>
  <files>apps/web/src/components/admin/image-lightbox.tsx</files>
  <action>
    Create `image-lightbox.tsx` — hand-rolled lightbox (per RESEARCH.md, no library needed):

    1. **Props interface**:
       ```typescript
       interface ImageLightboxProps {
         image: AdminImage | null;
         onClose: () => void;
         onDelete: (id: string) => Promise<void>;
       }
       ```

    2. **Overlay**: Fixed full-screen overlay with dark semi-transparent backdrop. Click backdrop to close. Escape key to close.

    3. **Image display**: Center the full-size image using `<img src="${API_URL}/uploads/${image.filePath}" />`. Use `max-w-[90vw] max-h-[80vh] object-contain` for responsive sizing.

    4. **Metadata panel** below image:
       - 크기: `${image.width} x ${image.height}`
       - 파일 크기: formatted (KB/MB)
       - 생성일: formatted date
       - 사용자: `image.userEmail`
       - 프로젝트: `image.projectName`

    5. **Delete button**: Red "삭제" button in metadata panel. On click:
       - Show `ConfirmDialog` (danger variant) with "이 이미지를 삭제하시겠습니까?" message
       - On confirm: call `onDelete(image.id)`, which triggers `adminApi.deleteImage` in parent
       - After delete: close lightbox, parent refreshes grid

    6. **State**: `showDeleteConfirm` boolean, `deleting` loading flag

    7. **Integration with ContentGrid**: When `selectedImage` is set, render `<ImageLightbox image={selectedImage} onClose={() => setSelectedImage(null)} onDelete={handleDeleteImage} />`. The `handleDeleteImage` function calls `adminApi.deleteImage(token, id)` and then refreshes the grid.

    8. Return null if `image` is null.

    9. Use `stopPropagation` on the lightbox content card to prevent closing when clicking inside.
  </action>
  <verify>
    <automated>cd /Users/sangwopark19/icons/icons-ai-mockup && npx turbo typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>Image lightbox shows full-size preview with metadata (size, resolution, date, user, project). Delete button opens confirmation dialog, then removes image from DB + filesystem. Lightbox closes after delete.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx turbo typecheck`
- Content tab shows image grid with filters and pagination
- Clicking image opens lightbox with metadata
- Individual delete from lightbox works with confirmation
- Bulk delete shows count and confirms before executing
- All tests still pass: `cd apps/api && npx vitest run`
</verification>

<success_criteria>
- Image grid displays thumbnails with user, date, project info
- All three filters (email, date range, project) work
- Lightbox shows full-size image with all metadata fields
- Individual delete removes image with confirmation
- Bulk delete shows matching count, confirms, then deletes all matching
- Grid auto-refreshes after any delete
</success_criteria>

<output>
After completion, create `.planning/phases/03-generation-and-content-monitoring/03-05-SUMMARY.md`
</output>
