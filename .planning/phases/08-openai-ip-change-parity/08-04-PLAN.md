---
phase: 08-openai-ip-change-parity
plan: 04
title: Phase 8 Smoke And Release Verification
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - .planning/phases/08-openai-ip-change-parity/08-SMOKE.md
autonomous: false
requirements: [PROV-01, OIP-01, OIP-02, OIP-03]
requirements_addressed: [PROV-01, OIP-01, OIP-02, OIP-03]
---

# Plan 08-04: Phase 8 Smoke And Release Verification

<objective>
Create the Phase 8 smoke checklist and run final automated and manual verification gates for the OpenAI IP Change v2 rollout.
</objective>

<threat_model>
## Threats

| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-08-11 | high | Real OpenAI smoke exposes API key or sample images in committed files | Keep `OPENAI_API_KEY` in environment only; do not commit sample inputs/outputs unless explicitly sanitized. |
| T-08-12 | medium | Phase passes automated checks but real GPT Image 2 rejects request shape | Gate real smoke on `OPENAI_API_KEY`; document request IDs and forbidden-parameter checks. |
| T-08-13 | medium | Product UI visually regresses on mobile or leaks provider names | Include browser checklist for desktop/mobile project, v2 form, result, and history surfaces. |
</threat_model>

<tasks>
<task id="08-04-01" type="execute">
  <title>Create Phase 8 smoke checklist</title>
  <read_first>
    - `.planning/phases/08-openai-ip-change-parity/08-SMOKE.md`
    - `.planning/phases/08-openai-ip-change-parity/08-VALIDATION.md`
    - `.planning/phases/08-openai-ip-change-parity/08-RESEARCH.md`
    - `.codex/skills/mockup-openai-cli-smoke/SKILL.md`
    - `.codex/skills/mockup-openai-cli-smoke/references/official-cli-notes.md`
    - `.codex/skills/mockup-openai-cli-smoke/scripts/images-edit.sh`
  </read_first>
  <action>
    Create `.planning/phases/08-openai-ip-change-parity/08-SMOKE.md` with these sections:
    - `## Automated Verification`
    - `## Browser Verification`
    - `## Real OpenAI Smoke`
    - `## Evidence To Record`

    Include exact automated commands:
    - `pnpm --filter @mockup-ai/api test`
    - `pnpm --filter @mockup-ai/api type-check`
    - `pnpm --filter @mockup-ai/web type-check`

    Include exact real-smoke prerequisites:
    - `OPENAI_API_KEY`
    - one source product image
    - one character reference image

    Include exact forbidden-parameter checklist:
    - `background: "transparent"` not sent
    - `input_fidelity` not sent
    - request IDs captured
    - output file path recorded

    Include browser checklist:
    - project page shows `IP 변경 v1` and `IP 변경 v2`
    - v2 form defaults preserve structure, fixed viewpoint, and fixed background enabled
    - result page shows `v2`, two candidates, save, download, and disabled follow-ups
    - history shows `v2` badge and reopens the result page
    - no product screen visibly shows `OpenAI`, `Gemini`, `GPT Image 2`, or `gpt-image-2`
  </action>
  <verify>
    - `grep -n \"Automated Verification\\|Real OpenAI Smoke\\|background: \\\"transparent\\\"\\|input_fidelity\" .planning/phases/08-openai-ip-change-parity/08-SMOKE.md`
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-openai-ip-change-parity/08-SMOKE.md` exists.
    - `08-SMOKE.md` contains `pnpm --filter @mockup-ai/api test`.
    - `08-SMOKE.md` contains `pnpm --filter @mockup-ai/web type-check`.
    - `08-SMOKE.md` contains `OPENAI_API_KEY`.
    - `08-SMOKE.md` contains `background: "transparent"`.
    - `08-SMOKE.md` contains `input_fidelity`.
    - `08-SMOKE.md` contains `request IDs captured`.
  </acceptance_criteria>
</task>

<task id="08-04-02" type="verify">
  <title>Run final automated checks</title>
  <read_first>
    - `package.json`
    - `apps/api/package.json`
    - `apps/web/package.json`
    - `.planning/phases/08-openai-ip-change-parity/08-VALIDATION.md`
    - `.planning/phases/08-openai-ip-change-parity/08-SMOKE.md`
  </read_first>
  <action>
    Run the final automated Phase 8 verification commands:
    - `pnpm --filter @mockup-ai/api test`
    - `pnpm --filter @mockup-ai/api type-check`
    - `pnpm --filter @mockup-ai/web type-check`

    If any command fails, fix the implementation before marking the plan complete. Do not replace failing tests with weaker assertions.
  </action>
  <verify>
    - `pnpm --filter @mockup-ai/api test`
    - `pnpm --filter @mockup-ai/api type-check`
    - `pnpm --filter @mockup-ai/web type-check`
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @mockup-ai/api test` exits 0.
    - `pnpm --filter @mockup-ai/api type-check` exits 0.
    - `pnpm --filter @mockup-ai/web type-check` exits 0.
  </acceptance_criteria>
</task>

<task id="08-04-03" type="manual">
  <title>Run gated real OpenAI and browser smoke</title>
  <read_first>
    - `.planning/phases/08-openai-ip-change-parity/08-SMOKE.md`
    - `.codex/skills/mockup-openai-cli-smoke/SKILL.md`
    - `.codex/skills/mockup-openai-cli-smoke/references/official-cli-notes.md`
    - `.codex/skills/mockup-openai-cli-smoke/scripts/images-edit.sh`
  </read_first>
  <action>
    If `OPENAI_API_KEY` and representative local sample images are available, run the real OpenAI Image API edit smoke using the repo's smoke guidance. Record:
    - request ID
    - source image path
    - character image path
    - output image path
    - selected quality value
    - confirmation that `background: "transparent"` was not sent
    - confirmation that `input_fidelity` was not sent

    Then run browser verification at desktop and mobile widths for project page, v2 form, result page, and history page. If `OPENAI_API_KEY` is unavailable, mark real-provider smoke as manual-needed in the execution summary, but still complete automated checks.
  </action>
  <verify>
    - Manual evidence recorded in `08-SUMMARY.md` during execution.
  </verify>
  <acceptance_criteria>
    - Execution summary contains either `OpenAI real smoke: passed` with request ID evidence or `OpenAI real smoke: manual_needed - OPENAI_API_KEY unavailable`.
    - Execution summary contains browser verification notes for project page, v2 form, result page, and history page.
    - Execution summary states product UI did not visibly show `OpenAI`, `Gemini`, `GPT Image 2`, or `gpt-image-2`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Final phase verification requires:

```bash
pnpm --filter @mockup-ai/api test
pnpm --filter @mockup-ai/api type-check
pnpm --filter @mockup-ai/web type-check
```

Real OpenAI smoke is gated by `OPENAI_API_KEY` and representative sample images.
</verification>

<success_criteria>
- Phase 8 has explicit automated and manual smoke evidence.
- Real OpenAI runtime behavior is validated when credentials are available.
- UI and lifecycle behavior are verified without exposing provider/model names to product users.
</success_criteria>

<must_haves>
- Do not commit API keys or sample image assets.
- Do not mark real OpenAI smoke as passed without request ID evidence.
- Record manual-needed status when live credentials are unavailable.
</must_haves>
