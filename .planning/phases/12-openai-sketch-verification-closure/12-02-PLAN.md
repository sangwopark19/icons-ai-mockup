---
phase: 12-openai-sketch-verification-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - .planning/phases/09-openai-sketch-to-real-parity/09-VERIFICATION.md
  - .planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md
autonomous: true
requirements:
  - PROV-02
  - OSR-01
  - OSR-02
  - OSR-03
must_haves:
  truths:
    - Fresh automated checks or explicit command results are recorded against Phase 9 verification.
    - Deterministic audit fallback proves Phase 9 requirements are no longer orphaned by missing verification artifact.
    - Stale deployment evidence is not treated as current-branch proof.
  artifacts:
    - .planning/phases/09-openai-sketch-to-real-parity/09-VERIFICATION.md
    - .planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md
  key_links:
    - 09-VERIFICATION.md -> 12-AUDIT-CHECK.md deterministic coverage check.
    - Automated API/web checks -> 09-VERIFICATION.md Automated Checks section.
---

<objective>
Record fresh automated verification and follow-up audit evidence for Phase 9 requirement closure.

Purpose: Prove that `PROV-02`, `OSR-01`, `OSR-02`, and `OSR-03` are no longer orphaned by the audit workflow once `09-VERIFICATION.md` exists.
Output: Updated `09-VERIFICATION.md` automated checks plus `.planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md`
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/v1.1-INTEGRATION-CHECK.md
@.planning/phases/09-openai-sketch-to-real-parity/09-VERIFICATION.md
@.planning/phases/09-openai-sketch-to-real-parity/09-SMOKE.md
@.planning/phases/09-openai-sketch-to-real-parity/09-SUMMARY.md
@.planning/phases/12-openai-sketch-verification-closure/12-VALIDATION.md
@.planning/phases/12-openai-sketch-verification-closure/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated and static verification checks</name>
  <read_first>
    - .planning/phases/09-openai-sketch-to-real-parity/09-VERIFICATION.md
    - .planning/phases/09-openai-sketch-to-real-parity/09-SMOKE.md
    - .planning/phases/12-openai-sketch-verification-closure/12-VALIDATION.md
    - apps/api/package.json
    - apps/web/package.json
    - apps/api/src/services/openai-image.service.ts
    - apps/api/src/services/background-removal.service.ts
    - apps/api/src/services/__tests__/openai-image.service.test.ts
    - apps/api/src/services/__tests__/background-removal.service.test.ts
    - apps/api/src/services/__tests__/generation.service.test.ts
    - apps/api/src/routes/__tests__/generation.routes.test.ts
    - apps/api/src/__tests__/worker.provider-continuation.test.ts
    - apps/web/src/app/projects/[id]/page.tsx
    - apps/web/src/app/projects/[id]/sketch-to-real/openai/page.tsx
    - apps/web/src/app/projects/[id]/generations/[genId]/page.tsx
    - apps/web/src/app/projects/[id]/history/page.tsx
  </read_first>
  <files>.planning/phases/09-openai-sketch-to-real-parity/09-VERIFICATION.md</files>
  <action>Run `pnpm --filter @mockup-ai/api test`, `pnpm --filter @mockup-ai/api type-check`, and `pnpm --filter @mockup-ai/web type-check`. Run static checks proving `openai-image.service.ts` does not send `background: "transparent"` or `input_fidelity`, the background-removal quality gates exist, worker post-processes OpenAI Sketch transparent requests through `removeUniformLightBackground()`, and web entry/form/result/history still expose v2 Sketch lifecycle. Update the `Automated Checks` or `Behavioral Spot-Checks` section in `09-VERIFICATION.md` with fresh pass/fail results and exact commands. If any command fails, record the failure exactly and do not mark the related requirement as passed. Do not paste raw terminal logs containing secrets; summarize only command, exit status, and test count if present.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api test && pnpm --filter @mockup-ai/api type-check && pnpm --filter @mockup-ai/web type-check && ! rg -n "background:\\s*['\\\"]transparent|input_fidelity" apps/api/src/services/openai-image.service.ts && rg -n "removeUniformLightBackground|hasTransparency|transparentBackground" apps/api/src/worker.ts && rg -n "스케치 실사화 v2|sketch-to-real/openai|provider:\\s*'openai'|providerModel:\\s*'gpt-image-2'|outputCount:\\s*2" 'apps/web/src/app/projects/[id]/page.tsx' 'apps/web/src/app/projects/[id]/sketch-to-real/openai/page.tsx' 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx' 'apps/web/src/app/projects/[id]/history/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `09-VERIFICATION.md` contains `pnpm --filter @mockup-ai/api test`.
    - `09-VERIFICATION.md` contains `pnpm --filter @mockup-ai/api type-check`.
    - `09-VERIFICATION.md` contains `pnpm --filter @mockup-ai/web type-check`.
    - `rg -n "background:\s*['\"]transparent|input_fidelity" apps/api/src/services/openai-image.service.ts` exits 1.
    - `rg -n "removeUniformLightBackground|hasTransparency|transparentBackground" apps/api/src/worker.ts` exits 0.
    - `rg -n "스케치 실사화 v2|sketch-to-real/openai|provider:\s*'openai'|providerModel:\s*'gpt-image-2'|outputCount:\s*2" 'apps/web/src/app/projects/[id]/page.tsx' 'apps/web/src/app/projects/[id]/sketch-to-real/openai/page.tsx' 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx' 'apps/web/src/app/projects/[id]/history/page.tsx'` exits 0.
  </acceptance_criteria>
  <done>Fresh automated and static evidence is reflected in `09-VERIFICATION.md` without unsupported OpenAI parameter or stale deployment claims.</done>
</task>

<task type="auto">
  <name>Task 2: Produce deterministic follow-up audit evidence</name>
  <read_first>
    - .planning/phases/09-openai-sketch-to-real-parity/09-VERIFICATION.md
    - .planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md (confirm missing before creating)
    - .planning/v1.1-MILESTONE-AUDIT.md
    - .planning/v1.1-INTEGRATION-CHECK.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <files>.planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md</files>
  <action>Create `.planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md`. First try any available local audit command that is safe and deterministic, such as `node /Users/sangwopark19/.codex/get-shit-done/bin/gsd-tools.cjs audit-open --json` or `node ./node_modules/@gsd-build/sdk/dist/cli.js query audit-uat --raw`; record only sanitized command status. Regardless of audit command availability, include a deterministic fallback section with this exact proof target: `09-VERIFICATION.md exists and names PROV-02, OSR-01, OSR-02, OSR-03`. Include a node fallback command that reads `09-VERIFICATION.md`, fails if any of those four IDs are missing, fails if `OSR-03` lacks `PARTIAL_WITH_MILESTONE_EXCEPTION`, `human_needed`, or `milestone exception`, and prints `Phase 9 verification coverage OK: PROV-02, OSR-01, OSR-02, OSR-03`. State that the previous `v1.1-MILESTONE-AUDIT.md` orphaned status is superseded for orphan detection by the new verification artifact, while `OSR-03` remains live-evidence human_needed/exception-scoped until final alpha/composite PNG evidence exists. Do not edit the historical audit artifact by hand.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/09-openai-sketch-to-real-parity/09-VERIFICATION.md';const s=fs.readFileSync(p,'utf8');const ids=['PROV-02','OSR-01','OSR-02','OSR-03'];for(const id of ids){if(!s.includes(id)){throw new Error(id+' missing from 09-VERIFICATION.md')}}if(!/(PARTIAL_WITH_MILESTONE_EXCEPTION|human_needed|milestone exception)/i.test(s)){throw new Error('OSR-03 exception/human_needed status missing')}if(/OSR-03\\s*\\|\\s*SATISFIED(?!_WITH_EXCEPTION)/.test(s)){throw new Error('OSR-03 is overclaimed as satisfied')}console.log('Phase 9 verification coverage OK: '+ids.join(', '))" && test -f .planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md && rg -n "Phase 9 verification coverage OK: PROV-02, OSR-01, OSR-02, OSR-03|not orphaned|PARTIAL_WITH_MILESTONE_EXCEPTION|human_needed|09-VERIFICATION.md exists" .planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md && ! rg -n "data:image|base64,|sk-[A-Za-z0-9]" .planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md` exists.
    - `12-AUDIT-CHECK.md` contains `Phase 9 verification coverage OK: PROV-02, OSR-01, OSR-02, OSR-03`.
    - `12-AUDIT-CHECK.md` contains `09-VERIFICATION.md exists`.
    - `12-AUDIT-CHECK.md` contains either `PARTIAL_WITH_MILESTONE_EXCEPTION` or `human_needed` for `OSR-03`.
    - The node fallback command exits 0.
    - `rg -n "data:image|base64,|sk-[A-Za-z0-9]" .planning/phases/12-openai-sketch-verification-closure/12-AUDIT-CHECK.md` exits 1.
  </acceptance_criteria>
  <done>Follow-up audit evidence proves Phase 9 IDs are no longer orphaned by missing verification, with OSR-03 explicitly exception/human-needed scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test/runtime commands -> markdown evidence | Command results are summarized into committed planning artifacts. |
| Existing audit artifact -> new closure evidence | A historical audit finding is superseded by deterministic follow-up proof, not silently edited. |
| Browser/deployment evidence -> requirement status | Current-branch proof must not come from stale deployment/runtime targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | `09-VERIFICATION.md`, `12-AUDIT-CHECK.md` | mitigate | Record command status, request IDs, relative output paths, and derived metrics only; acceptance rejects `data:image`, `base64,`, and `sk-` key-like strings. |
| T-12-06 | Repudiation | Follow-up audit closure | mitigate | Create `12-AUDIT-CHECK.md` with a deterministic node fallback proving all four IDs exist in `09-VERIFICATION.md`. |
| T-12-07 | Tampering / Repudiation | `OSR-03` closure status | mitigate | Node fallback fails if `OSR-03` is overclaimed as `SATISFIED` without `_WITH_EXCEPTION`; artifact must contain exception/human-needed status. |
| T-12-08 | Spoofing | Browser/deployment evidence | mitigate | Audit evidence must distinguish current-branch Docker/source evidence from the stale `100.69.75.47:3000` runtime noted in Phase 9. |
</threat_model>

<verification>
Run the full automated command chain in Task 1 and the deterministic fallback audit proof in Task 2. If the exact GSD milestone audit command is unavailable, that is acceptable only when `12-AUDIT-CHECK.md` records the fallback command and its successful output.
</verification>

<success_criteria>
- `09-VERIFICATION.md` contains fresh automated/static check results.
- `12-AUDIT-CHECK.md` exists and proves `09-VERIFICATION.md` names `PROV-02`, `OSR-01`, `OSR-02`, and `OSR-03`.
- `OSR-03` remains exception/human-needed scoped unless final alpha/composite PNG evidence exists.
- No historical audit artifact is hand-edited to hide previous findings.
- No secrets, raw approved image data, base64 payloads, or raw vendor responses are committed.
</success_criteria>

<output>
After completion, create `.planning/phases/12-openai-sketch-verification-closure/12-02-SUMMARY.md`
</output>
