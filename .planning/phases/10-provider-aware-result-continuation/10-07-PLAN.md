---
phase: 10-provider-aware-result-continuation
plan: 07
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - .planning/phases/10-provider-aware-result-continuation/10-SMOKE.md
autonomous: true
requirements: [PROV-03, PROV-04, OED-01, OED-02, OED-03]
user_setup:
  - service: openai
    why: "Live continuation smoke for GPT Image 2 partial edit and style-copy when active account access is available"
    env_vars:
      - name: OPENAI_API_KEY
        source: "Existing DB-managed active OpenAI key or local operator environment for direct CLI smoke"
    dashboard_config:
      - task: "Ensure the OpenAI organization has access to `gpt-image-2` and the configured Responses image-tool model"
        location: "OpenAI account/project settings"
must_haves:
  truths:
    - "All Phase 10 requirements have automated unit/type coverage."
    - "Product UI raw metadata boundary is checked by static grep and browser/manual checklist."
    - "OpenAI live smoke records request IDs when active DB-managed OpenAI key and representative images are available."
    - "No Prisma schema file was modified in Phase 10; schema push is not required."
  artifacts:
    - path: ".planning/phases/10-provider-aware-result-continuation/10-SMOKE.md"
      provides: "Phase 10 smoke checklist and evidence record"
  key_links:
    - from: "10-SMOKE.md"
      to: "Phase 10 plans"
      via: "automated command evidence and smoke status"
      pattern: "Provider-Aware Result Continuation Smoke"
---

<objective>
Close Phase 10 with automated verification and gated OpenAI smoke evidence.

Purpose: Research identified live OpenAI account/model access as a medium risk. This plan records deterministic automated evidence and live smoke status when available, without fabricating runtime proof.
Output: `10-SMOKE.md` containing automated checks, raw metadata boundary checks, provider-isolation checks, and live-smoke evidence or `manual_needed` reasons.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/OPENAI-SKILL-GUARDRAILS.md
@.planning/phases/10-provider-aware-result-continuation/10-CONTEXT.md
@.planning/phases/10-provider-aware-result-continuation/10-RESEARCH.md
@.planning/phases/10-provider-aware-result-continuation/10-VALIDATION.md
@.codex/skills/mockup-openai-cli-smoke/SKILL.md
@.codex/skills/mockup-openai-cli-smoke/references/official-cli-notes.md
@.planning/phases/09-openai-sketch-to-real-parity/09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 10 smoke checklist</name>
  <files>.planning/phases/10-provider-aware-result-continuation/10-SMOKE.md</files>
  <read_first>
    - .planning/phases/10-provider-aware-result-continuation/10-VALIDATION.md
    - .planning/phases/10-provider-aware-result-continuation/10-RESEARCH.md
    - .planning/OPENAI-SKILL-GUARDRAILS.md
    - .codex/skills/mockup-openai-cli-smoke/SKILL.md
    - .codex/skills/mockup-openai-cli-smoke/references/official-cli-notes.md
  </read_first>
  <action>
    Create `10-SMOKE.md` with heading `# Phase 10 Provider-Aware Result Continuation Smoke`. Include sections with these exact headings:
    - `## Automated Verification`
    - `## Product UI Metadata Boundary`
    - `## Provider-Pinned Regeneration`
    - `## OpenAI Partial Edit Smoke`
    - `## OpenAI Style Copy Smoke`
    - `## Gemini/OpenAI Lineage Isolation`
    - `## Schema Push Status`
    - `## Evidence Status`

    Under `Schema Push Status`, write `No Phase 10 plan modifies apps/api/prisma/schema.prisma; npx prisma db push is not required.` unless execution actually changed schema files. If schema files were modified despite the plans, stop and run `npx prisma db push` before completing verification.

    Include command checklist entries for:
    `pnpm --filter @mockup-ai/api test`,
    `pnpm --filter @mockup-ai/api type-check`,
    `pnpm --filter @mockup-ai/web type-check`,
    product-copy grep for `Gemini|OpenAI|GPT Image 2|gpt-image-2|providerModel`,
    worker grep for `thoughtSignature`,
    and OpenAI forbidden-parameter grep for `background:` and `input_fidelity`.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-provider-aware-result-continuation/10-SMOKE.md</automated>
    <automated>rg -n "Provider-Aware Result Continuation Smoke|Automated Verification|Product UI Metadata Boundary|OpenAI Partial Edit Smoke|OpenAI Style Copy Smoke|Schema Push Status|npx prisma db push is not required" .planning/phases/10-provider-aware-result-continuation/10-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `10-SMOKE.md` exists.
    - `10-SMOKE.md` contains all eight exact section headings.
    - `10-SMOKE.md` states schema push status explicitly.
  </acceptance_criteria>
  <done>Phase 10 has a concrete smoke evidence artifact before final verification commands run.</done>
</task>

<task type="auto">
  <name>Task 2: Run automated Phase 10 verification and record results</name>
  <files>.planning/phases/10-provider-aware-result-continuation/10-SMOKE.md</files>
  <read_first>
    - .planning/phases/10-provider-aware-result-continuation/10-SMOKE.md
    - .planning/phases/10-provider-aware-result-continuation/10-VALIDATION.md
    - apps/api/src/services/openai-image.service.ts
    - apps/api/src/worker.ts
    - apps/web/src/app/projects/[id]/generations/[genId]/page.tsx
    - apps/web/src/app/projects/[id]/history/page.tsx
    - apps/web/src/app/projects/[id]/style-copy/openai/page.tsx
  </read_first>
  <action>
    Run these commands and record each exit status in `10-SMOKE.md` under `Automated Verification`:
    - `pnpm --filter @mockup-ai/api test`
    - `pnpm --filter @mockup-ai/api type-check`
    - `pnpm --filter @mockup-ai/web type-check`

    Run and record static checks:
    - `rg -n "style-copy/openai|selectedImageId|수정 결과" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx'`
    - `rg -n "스타일 기준 · v2|copy-style|copyTarget: 'ip-change'|copyTarget: 'new-product'" 'apps/web/src/app/projects/[id]/style-copy/openai/page.tsx'`
    - `! rg -n "Gemini|OpenAI|GPT Image 2|gpt-image-2|providerModel" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx' 'apps/web/src/app/projects/[id]/history/page.tsx' 'apps/web/src/app/projects/[id]/style-copy/openai/page.tsx'`
    - `rg -n "generateStyleCopyWithLinkage|generateStyleCopyFromImage" apps/api/src/worker.ts`
    - `! rg -n "background:|input_fidelity" apps/api/src/services/openai-image.service.ts`

    If a grep command intentionally finds code-only provider branching such as `provider === 'openai'`, record it as allowed only when it is not visible product copy. Do not mark raw visible product labels as passed.
  </action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api test && pnpm --filter @mockup-ai/api type-check && pnpm --filter @mockup-ai/web type-check</automated>
    <automated>rg -n "api test|api type-check|web type-check|Product UI Metadata Boundary|Gemini/OpenAI Lineage Isolation" .planning/phases/10-provider-aware-result-continuation/10-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `10-SMOKE.md` records pass/fail status for API tests, API type-check, and web type-check.
    - `10-SMOKE.md` records product UI metadata boundary grep results.
    - `10-SMOKE.md` records worker lineage isolation grep results.
  </acceptance_criteria>
  <done>Automated Phase 10 verification evidence is recorded and full suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Run gated live OpenAI continuation smoke when available</name>
  <files>.planning/phases/10-provider-aware-result-continuation/10-SMOKE.md</files>
  <read_first>
    - .planning/phases/10-provider-aware-result-continuation/10-SMOKE.md
    - .codex/skills/mockup-openai-cli-smoke/SKILL.md
    - .codex/skills/mockup-openai-cli-smoke/references/official-cli-notes.md
    - .planning/phases/09-openai-sketch-to-real-parity/09-SUMMARY.md
  </read_first>
  <action>
    Determine whether live smoke prerequisites are available: active DB-managed OpenAI API key or `OPENAI_API_KEY`, a completed OpenAI result generation with selected image, and representative target images approved for OpenAI transmission. If prerequisites are unavailable, record `manual_needed` with the exact missing prerequisite in `10-SMOKE.md`; do not fabricate request IDs or output paths.

    When prerequisites are available, run one OpenAI partial edit smoke and one OpenAI style-copy smoke through the app/API path if a local stack is running. Record generation IDs, request IDs, response IDs, image call IDs, output image count, selected image ID, and confirmation that no Gemini `thoughtSignature` was used for OpenAI. If only direct CLI smoke is available, use `mockup-openai-cli-smoke` Node/curl guidance and record that it validates provider runtime but not full app persistence.

    For every live OpenAI smoke attempt, record whether `background: "transparent"` and `input_fidelity` were absent. Record OpenAI 403/account-model-access failures with request ID when present. Do not treat OpenAI account access failure as Gemini fallback permission.
  </action>
  <verify>
    <automated>rg -n "OpenAI Partial Edit Smoke|OpenAI Style Copy Smoke|request ID|manual_needed|background: \"transparent\"|input_fidelity" .planning/phases/10-provider-aware-result-continuation/10-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `10-SMOKE.md` contains either live request/output evidence or `manual_needed` with missing prerequisites.
    - Any OpenAI failure includes request ID when available.
    - The smoke record states OpenAI continuation did not fall back to Gemini.
  </acceptance_criteria>
  <done>Live OpenAI continuation risk is either evidenced or explicitly recorded as gated/manual-needed.</done>
</task>

</tasks>

<must_haves>
- Every Phase 10 requirement ID appears in this plan and in at least one implementation plan.
- OpenAI live smoke is attempted only when prerequisites are available and is never fabricated.
- Schema push status is explicit because schema files are intentionally not modified by Phase 10 plans.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local/test runtime -> OpenAI | Smoke tests may transmit approved images and prompts to OpenAI. |
| Verification artifact -> repository | Smoke evidence records request IDs and paths without secrets or raw image bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Information Disclosure | smoke artifact | mitigate | Record request IDs and output paths only; do not record API keys, raw base64, or raw vendor bodies. |
| T-10-07-02 | Repudiation | verification evidence | mitigate | Record exact commands, exit status, generation IDs, and request IDs when available. |
| T-10-07-03 | Provider Confusion | live smoke | mitigate | Smoke evidence must state no Gemini fallback occurred for OpenAI failures. |
| T-10-07-04 | Tampering | schema state | mitigate | If schema files changed, run `npx prisma db push` before verification; otherwise record no schema push required. |
</threat_model>

<verification>
Full phase verification command: `pnpm --filter @mockup-ai/api test && pnpm --filter @mockup-ai/api type-check && pnpm --filter @mockup-ai/web type-check`.
</verification>

<success_criteria>
- `10-SMOKE.md` exists and contains automated, static, and live/manual-needed evidence.
- Full API tests, API type-check, and web type-check are green.
- Product UI raw metadata boundary and OpenAI/Gemini lineage isolation are recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/10-provider-aware-result-continuation/10-07-SUMMARY.md`.
</output>
