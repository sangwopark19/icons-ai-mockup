---
phase: 10-provider-aware-result-continuation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/services/openai-image.service.ts
  - apps/api/src/services/__tests__/openai-image.service.test.ts
autonomous: true
requirements: [OED-01, OED-02, OED-03]
must_haves:
  truths:
    - "D-10/D-11/D-12/D-14: OpenAI partial edit uses Image API edit, strict preserve prompt, exactly one output, and OpenAI metadata."
    - "D-18/D-19/D-20/D-21/D-22: OpenAI style copy uses linkage first, selected-image fallback second, returns exactly two candidates, and never depends on Gemini `thoughtSignature`."
  artifacts:
    - path: "apps/api/src/services/openai-image.service.ts"
      provides: "OpenAI partial edit and style-copy runtime helpers"
    - path: "apps/api/src/services/__tests__/openai-image.service.test.ts"
      provides: "Prompt, output-count, metadata, and forbidden-parameter tests"
  key_links:
    - from: "apps/api/src/services/openai-image.service.ts"
      to: "OpenAI Images API"
      via: "client.images.edit for partial edit and fallback style copy"
      pattern: "images.edit"
    - from: "apps/api/src/services/openai-image.service.ts"
      to: "OpenAI Responses API"
      via: "client.responses.create for linkage-aware style copy"
      pattern: "responses.create"
---

<objective>
Add the OpenAI service methods that provider-aware continuations depend on.

Purpose: Backend routes and worker logic need a provider-isolated runtime boundary before they can enable OpenAI partial edit and style-copy flows.
Output: Tested `OpenAIImageService` methods for one-output partial edit, two-candidate style-copy fallback, and two-candidate linkage-aware style copy.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/OPENAI-SKILL-GUARDRAILS.md
@.planning/phases/10-provider-aware-result-continuation/10-CONTEXT.md
@.planning/phases/10-provider-aware-result-continuation/10-RESEARCH.md
@.planning/phases/10-provider-aware-result-continuation/10-PATTERNS.md
@.planning/phases/10-provider-aware-result-continuation/10-VALIDATION.md
@.codex/skills/mockup-openai-workflows/SKILL.md
@.codex/skills/mockup-openai-workflows/references/workflow-matrix.md
@.codex/skills/mockup-openai-workflows/references/prompt-playbook.md
@.codex/skills/mockup-openai-image-runtime/SKILL.md
@.codex/skills/mockup-openai-image-runtime/references/endpoint-matrix.md
@.codex/skills/mockup-openai-image-runtime/references/node-runtime.md
@.codex/skills/mockup-precision-edit/SKILL.md
@.codex/skills/mockup-precision-edit/references/gpt-image-2-notes.md

<interfaces>
Current service result contract:
```ts
export interface OpenAIImageGenerationResult {
  images: Buffer[];
  requestIds: string[];
  responseId?: string;
  imageCallIds: string[];
  revisedPrompt?: string;
  providerTrace: Record<string, unknown>;
}
```

Current forbidden-parameter test pattern:
```ts
expect(firstCall.background).toBeUndefined();
expect(firstCall.input_fidelity).toBeUndefined();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one-output OpenAI partial edit helper</name>
  <files>apps/api/src/services/openai-image.service.ts, apps/api/src/services/__tests__/openai-image.service.test.ts</files>
  <read_first>
    - apps/api/src/services/openai-image.service.ts
    - apps/api/src/services/__tests__/openai-image.service.test.ts
    - .codex/skills/mockup-precision-edit/references/gpt-image-2-notes.md
    - .codex/skills/mockup-openai-workflows/references/prompt-playbook.md
  </read_first>
  <behavior>
    - D-10: Prompt includes `Must change:`, `Must preserve exactly:`, and `Hard constraints:` sections.
    - D-11: Runtime uses `client.images.edit()`.
    - D-12: Runtime sets `n: 1` and throws if exactly one image is not returned.
    - D-14: Result `providerTrace` includes `{ provider: 'openai', model: 'gpt-image-2', endpoint: 'images.edit', workflow: 'partial_edit', outputCount: 1 }`.
  </behavior>
  <action>
    Add `OpenAIPartialEditOptions` with `quality?: OpenAIQuality` and `userPrompt: string`. Add `generatePartialEdit(apiKey: string, selectedImageBase64: string, userPrompt: string, options?: { quality?: OpenAIQuality }): Promise<OpenAIImageGenerationResult>`.

    The method must decode the selected result image, call `client.images.edit({ model: 'gpt-image-2', image: selectedImage, prompt, quality: options?.quality ?? 'medium', n: 1, size: '1024x1024', output_format: 'png' })`, and omit `background` and `input_fidelity`. Build the prompt with these exact sections:
    `Task:`, `Must change:`, `Must preserve exactly:`, `Hard constraints:`, `Output:`.
    The preserve list must include this exact phrase: `Product body, camera angle, crop, background rule, lighting, text, labels, hardware, and non-target details.`

    Extend `openai-image.service.test.ts` so mocks cover the one-image response and assert `images` length is 1, `n` is 1, `endpoint` is `images.edit`, `workflow` is `partial_edit`, `background` is undefined, and `input_fidelity` is undefined.
  </action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api test -- src/services/__tests__/openai-image.service.test.ts</automated>
    <automated>rg -n "generatePartialEdit|workflow: 'partial_edit'|Must preserve exactly|Product body, camera angle, crop, background rule, lighting, text, labels, hardware, and non-target details|n: 1" apps/api/src/services/openai-image.service.ts apps/api/src/services/__tests__/openai-image.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `generatePartialEdit` exists and calls `client.images.edit`.
    - Tests assert `n: 1`, one returned buffer, no `background`, and no `input_fidelity`.
    - The prompt contains the exact preserve phrase required by D-10.
  </acceptance_criteria>
  <done>OpenAI partial edit runtime is available and tested as a one-output Image API edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add linkage-first and fallback OpenAI style-copy helpers</name>
  <files>apps/api/src/services/openai-image.service.ts, apps/api/src/services/__tests__/openai-image.service.test.ts</files>
  <read_first>
    - apps/api/src/services/openai-image.service.ts
    - apps/api/src/services/__tests__/openai-image.service.test.ts
    - .codex/skills/mockup-openai-image-runtime/references/endpoint-matrix.md
    - .codex/skills/mockup-openai-image-runtime/references/node-runtime.md
    - .codex/skills/mockup-precision-edit/references/gpt-image-2-notes.md
  </read_first>
  <behavior>
    - D-18: `generateStyleCopyWithLinkage` uses saved `openaiResponseId` or `openaiImageCallId` before fallback.
    - D-19: `generateStyleCopyFromImage` uses selected result image plus new target image when linkage is missing.
    - D-20: both style-copy helpers return exactly two candidates.
    - D-21: no style-copy helper accepts or reads `thoughtSignature`.
    - D-22: prompt preserves approved output composition, viewpoint, lighting, background, product treatment, and polish while replacing only the named target.
  </behavior>
  <action>
    Add `type OpenAIStyleCopyTarget = 'ip-change' | 'new-product'`. Add `generateStyleCopyFromImage(apiKey, styleImageBase64, targetImageBase64, options)` using `client.images.edit()` with two input images, `n: 2`, `size: '1024x1024'`, `output_format: 'png'`, and no `background` or `input_fidelity`.

    Add `generateStyleCopyWithLinkage(apiKey, targetImageBase64, linkage, options)` using `client.responses.create()` with `model: process.env.OPENAI_RESPONSES_IMAGE_MODEL ?? 'gpt-5.4'`, `previous_response_id: linkage.openaiResponseId` when present, and `tools: [{ type: 'image_generation', action: 'edit', quality }]`. Store the Responses top-level model in `providerTrace.responsesModel` while keeping `providerTrace.model` as `gpt-image-2`. If neither `linkage.openaiResponseId` nor `linkage.openaiImageCallId` is present, throw `OpenAI 스타일 복사 linkage가 없습니다`.

    Build style-copy prompts with exact sections `Task:`, `Image roles:`, `Must change:`, `Must preserve:`, `Hard constraints:`, `Output:`. For `copyTarget === 'ip-change'`, `Must change:` must say `Replace only the character/IP artwork.` For `copyTarget === 'new-product'`, it must say `Replace only the product.` The preserve section must include `composition, viewpoint, lighting, background, product treatment, and polish`.

    Extend OpenAI SDK mocks to expose `responses.create`. Add tests for linkage path (`responses.create` called, two buffers, response ID and image call IDs captured), fallback path (`images.edit` called with two images and `n: 2`), no forbidden parameters, and no `thoughtSignature` token in the service file.
  </action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api test -- src/services/__tests__/openai-image.service.test.ts</automated>
    <automated>rg -n "generateStyleCopyWithLinkage|generateStyleCopyFromImage|OPENAI_RESPONSES_IMAGE_MODEL|responses\\.create|copyTarget|Replace only the character/IP artwork|Replace only the product" apps/api/src/services/openai-image.service.ts apps/api/src/services/__tests__/openai-image.service.test.ts</automated>
    <automated>! rg -n "thoughtSignature" apps/api/src/services/openai-image.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `generateStyleCopyWithLinkage` contains `responses.create` and records `responsesModel` in `providerTrace`.
    - `generateStyleCopyFromImage` contains `images.edit` with `n: 2`.
    - Tests prove both helpers return two images and never send `background` or `input_fidelity`.
    - `rg -n "thoughtSignature" apps/api/src/services/openai-image.service.ts` returns no matches.
  </acceptance_criteria>
  <done>OpenAI style-copy runtime supports linkage-first and selected-image fallback execution without Gemini lineage primitives.</done>
</task>

</tasks>

<must_haves>
- OED-01 has a tested OpenAI one-shot edit method.
- OED-02 has tested OpenAI style-copy methods for linkage and selected-image fallback.
- OED-03 is protected at the service boundary because OpenAI helper signatures do not include Gemini `thoughtSignature`.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> OpenAI | User prompts and images leave the app boundary for OpenAI processing. |
| OpenAI -> API | OpenAI response IDs, request IDs, image call IDs, revised prompts, and generated images return to persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | partial edit prompt | mitigate | Wrap user prompt in fixed `Must change`, `Must preserve exactly`, and `Hard constraints` sections that override conflicting user text. |
| T-10-02-02 | Information Disclosure | OpenAI metadata handling | mitigate | Store request/response/image-call IDs and providerTrace only; never expose raw API keys, uploaded bytes, base64, or raw vendor bodies. |
| T-10-02-03 | Tampering | style-copy lineage | mitigate | Use OpenAI linkage first and selected-image fallback second; service accepts no Gemini `thoughtSignature`. |
| T-10-02-04 | Denial of Service | OpenAI account/model access | accept | Live account access is verified in Plan 07 smoke when available; service fails clearly without Gemini fallback. |
</threat_model>

<verification>
Run `pnpm --filter @mockup-ai/api test -- src/services/__tests__/openai-image.service.test.ts` and the greps in each task.
</verification>

<success_criteria>
- OpenAI partial edit returns exactly one image.
- OpenAI style copy returns exactly two images for linkage and fallback paths.
- Service tests assert prompt sections, forbidden parameter omission, and OpenAI metadata capture.
</success_criteria>

<output>
After completion, create `.planning/phases/10-provider-aware-result-continuation/10-02-SUMMARY.md`.
</output>
