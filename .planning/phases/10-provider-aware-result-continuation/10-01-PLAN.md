---
phase: 10-provider-aware-result-continuation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/projects/[id]/generations/[genId]/page.tsx
  - apps/web/src/app/projects/[id]/history/page.tsx
autonomous: true
requirements: [PROV-03, OED-01, OED-02]
must_haves:
  truths:
    - "D-01/D-02: result and history product UI show only `v1` or `v2` for provider lane."
    - "D-03/D-04: follow-up controls use persisted `generation.provider` and selected image state, not raw vendor labels or query-only state."
    - "D-09/D-13: the existing partial edit modal is available for OpenAI results and routes to the returned generation."
    - "D-15/D-16/D-17: OpenAI style-copy actions route to the dedicated `/projects/:id/style-copy/openai` page with style reference, copy target, and selected image id."
  artifacts:
    - path: "apps/web/src/app/projects/[id]/generations/[genId]/page.tsx"
      provides: "Provider-derived result badge, enabled continuation actions, one-result edit display"
    - path: "apps/web/src/app/projects/[id]/history/page.tsx"
      provides: "Provider-derived history badge and product-copy boundary"
  key_links:
    - from: "apps/web/src/app/projects/[id]/generations/[genId]/page.tsx"
      to: "/api/generations/:id/edit"
      via: "POST with `prompt` and `selectedImageId`"
      pattern: "selectedImageId"
    - from: "apps/web/src/app/projects/[id]/generations/[genId]/page.tsx"
      to: "/projects/:id/style-copy/openai"
      via: "router.push with `styleRef`, `copyTarget`, `imageId`"
      pattern: "style-copy/openai"
---

<objective>
Enable provider-aware result and history product UI without exposing raw provider/model labels.

Purpose: PROV-03 requires users to see provider lineage, but Phase 10 locks the product signal to `v1`/`v2`. This plan removes the Phase 8/9 disabled OpenAI follow-up state from the result page and makes result/history actions derive behavior from persisted generation data.
Output: Updated result and history pages that keep raw vendor/model strings out of product UI while routing OpenAI continuations to provider-pinned backend flows.
</objective>

<execution_context>
@/Users/sangwopark19/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sangwopark19/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/OPENAI-SKILL-GUARDRAILS.md
@.planning/phases/10-provider-aware-result-continuation/10-CONTEXT.md
@.planning/phases/10-provider-aware-result-continuation/10-RESEARCH.md
@.planning/phases/10-provider-aware-result-continuation/10-PATTERNS.md
@.planning/phases/10-provider-aware-result-continuation/10-UI-SPEC.md
@.planning/phases/10-provider-aware-result-continuation/10-VALIDATION.md
@.codex/skills/mockup-openai-dual-provider/SKILL.md
@.codex/skills/mockup-openai-workflows/SKILL.md
@.codex/skills/mockup-precision-edit/SKILL.md

<interfaces>
From `apps/web/src/lib/api.ts`:
```ts
export type GenerationProvider = 'gemini' | 'openai';
export type GenerationMode = 'ip_change' | 'sketch_to_real';
export interface GenerationDetail {
  id: string;
  status: 'pending' | 'processing' | 'completed' | 'failed';
  mode: GenerationMode;
  provider: 'gemini' | 'openai';
  providerModel: string;
  options?: Record<string, unknown>;
  errorMessage: string | null;
  images: GenerationImage[];
}
```

Current result-page blockers to remove:
```ts
if (isV2) return; // in handleEdit, handleRegenerateWithSameInputs, handleStyleCopy
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enable provider-pinned result follow-up actions</name>
  <files>apps/web/src/app/projects/[id]/generations/[genId]/page.tsx</files>
  <read_first>
    - apps/web/src/app/projects/[id]/generations/[genId]/page.tsx
    - apps/web/src/lib/api.ts
    - .planning/phases/10-provider-aware-result-continuation/10-CONTEXT.md
    - .planning/phases/10-provider-aware-result-continuation/10-UI-SPEC.md
    - .planning/phases/10-provider-aware-result-continuation/10-PATTERNS.md
  </read_first>
  <behavior>
    - D-01/D-02: header badge text is exactly `v1` for `provider !== 'openai'` and `v2` for `provider === 'openai'`.
    - D-09/D-13: `부분 수정` opens the existing modal for OpenAI results, posts `prompt` and `selectedImageId`, and routes to `/projects/${projectId}/generations/${data.data.generationId}`.
    - D-15/D-16/D-17: OpenAI style-copy buttons route to `/projects/${projectId}/style-copy/openai?styleRef=${generation.id}&copyTarget=ip-change|new-product&imageId=${selectedImageId}`.
    - D-05/D-08: `동일 조건 재생성` posts to `/api/generations/${genId}/regenerate` for both providers and displays the UI-SPEC missing-input error instead of falling back.
  </behavior>
  <action>
    Remove the `isV2` early returns and disabled v2 follow-up notice. Keep the button order exactly: `부분 수정`, `히스토리에 저장`, `스타일 복사 (IP 변경)`, `스타일 복사 (새 제품 적용)`, `동일 조건 재생성`, `조건 수정`.

    In `handleEdit`, require `accessToken`, `selectedImageId`, and non-empty `editPrompt`; send:
    `{ "prompt": editPrompt, "selectedImageId": selectedImageId }`
    to `POST /api/generations/${genId}/edit`. Use these exact modal strings from UI-SPEC: body `선택한 결과에서 요청한 부분만 수정합니다. 제품 구조, 시점, 배경, 부자재, 라벨은 유지됩니다.`, placeholder `예: 캐릭터 색상만 파란색으로 변경해주세요`, cancel `수정 취소`, submit `수정 요청`, loading `수정 결과 생성 중...`, empty prompt error `수정할 내용을 입력해주세요.`, submit error `부분 수정 요청에 실패했습니다. 선택 이미지와 요청 내용을 확인한 뒤 다시 시도해주세요.`

    In `handleStyleCopy`, branch only on persisted `generation.provider`. For `provider === 'openai'`, require `selectedImageId` and route to `/projects/${projectId}/style-copy/openai` with query params `styleRef`, `copyTarget`, and `imageId`. For Gemini, preserve the existing `/projects/${projectId}/ip-change?styleRef=...&copyTarget=...` route.

    In the image count heading, render `수정 결과` when `generation.provider === 'openai'` and `orderedImages.length === 1`; otherwise keep `생성된 이미지 (2개)` for OpenAI two-candidate results and `생성된 이미지 (${orderedImages.length}개)` for Gemini. Do not render raw strings `Gemini`, `OpenAI`, `GPT Image 2`, `gpt-image-2`, `provider`, or `providerModel` as product copy.
  </action>
  <verify>
    <automated>pnpm --filter @mockup-ai/web type-check</automated>
    <automated>rg -n "style-copy/openai|selectedImageId|수정 결과|수정 결과 생성 중|원본 입력을 확인할 수 없어 동일 조건 재생성을 시작하지 못했습니다" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx'</automated>
    <automated>! rg -n "v2 후속 편집은 다음 업데이트에서 지원됩니다|disabled=\\{isV2\\}|if \\(isV2\\) return" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "style-copy/openai" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx'` finds the OpenAI dedicated style-copy route.
    - `rg -n "selectedImageId" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx'` finds it in the edit request body and style-copy query params.
    - `rg -n "수정 결과" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx'` finds the one-result edit heading.
    - `rg -n "Gemini|OpenAI|GPT Image 2|gpt-image-2|providerModel" 'apps/web/src/app/projects/[id]/generations/[genId]/page.tsx'` has no product-copy matches; `provider` may appear only in data-branching expressions such as `generation.provider === 'openai'`.
  </acceptance_criteria>
  <done>OpenAI result pages expose working follow-up controls, use `v1`/`v2` labels only, and route continuation requests with selected-image context.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten history card product metadata boundary</name>
  <files>apps/web/src/app/projects/[id]/history/page.tsx</files>
  <read_first>
    - apps/web/src/app/projects/[id]/history/page.tsx
    - apps/web/src/lib/api.ts
    - .planning/phases/10-provider-aware-result-continuation/10-UI-SPEC.md
    - .planning/phases/09-openai-sketch-to-real-parity/09-03-SUMMARY.md
  </read_first>
  <action>
    Keep history card version badges derived only from `item.provider === 'openai' ? 'v2' : 'v1'` for `ip_change` and `sketch_to_real`. Update the empty state body to the exact UI-SPEC copy `목업 결과를 저장하면 여기에서 다시 열고 후속 작업을 이어갈 수 있습니다.` Add `aria-label="히스토리 삭제"` to the delete button while preserving `e.preventDefault()` and `e.stopPropagation()`.

    Do not add raw `Gemini`, `OpenAI`, `GPT Image 2`, `gpt-image-2`, `provider`, or `providerModel` text to normal product UI. Do not add shadcn or registry components; continue using local `Button`.
  </action>
  <verify>
    <automated>pnpm --filter @mockup-ai/web type-check</automated>
    <automated>rg -n "item\\.provider === 'openai' \\? 'v2' : 'v1'|목업 결과를 저장하면 여기에서 다시 열고 후속 작업을 이어갈 수 있습니다|aria-label=\"히스토리 삭제\"" 'apps/web/src/app/projects/[id]/history/page.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - The history page contains the exact badge expression `item.provider === 'openai' ? 'v2' : 'v1'`.
    - The history empty state contains `목업 결과를 저장하면 여기에서 다시 열고 후속 작업을 이어갈 수 있습니다.`
    - The delete button has `aria-label="히스토리 삭제"` and still calls both `preventDefault()` and `stopPropagation()`.
  </acceptance_criteria>
  <done>History cards preserve v1/v2 product labeling and safe card deletion behavior.</done>
</task>

</tasks>

<must_haves>
- PROV-03 is covered by visible `v1`/`v2` badges in result and history pages.
- OED-01 is prepared by enabling the existing edit modal for OpenAI results and passing `selectedImageId`.
- OED-02 is prepared by routing OpenAI style-copy actions to the dedicated style-copy page with `styleRef`, `copyTarget`, and `imageId`.
- Deferred ideas are excluded: no provider comparison UI, no region mask UI, no iterative partial-edit refinement.
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | User-triggered continuation actions send generation IDs, selected image IDs, prompt text, and copy target values. |
| API data -> Product UI | Persisted provider/model fields cross into product screens and must be rendered as `v1`/`v2` only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | result/history UI | mitigate | Render only `v1`/`v2`; acceptance grep blocks visible `Gemini`, `OpenAI`, `GPT Image 2`, `gpt-image-2`, and `providerModel` strings. |
| T-10-01-02 | Tampering | result follow-up routing | mitigate | Route style-copy with `styleRef`, `copyTarget`, and `imageId`; backend plans validate ownership and persisted provider before execution. |
| T-10-01-03 | Elevation of Privilege | client provider state | mitigate | Do not infer runtime from badge/query/client state; UI branches from fetched `generation.provider`, backend remains source of truth. |
| T-10-01-04 | Information Disclosure | OpenAI trace metadata | accept | This plan does not expose trace fields; raw OpenAI support IDs remain API/admin/debug only per D-03. |
</threat_model>

<verification>
Run `pnpm --filter @mockup-ai/web type-check`. Then run the task greps above and confirm no disabled OpenAI follow-up text remains in the result page.
</verification>

<success_criteria>
- Result page displays `v1`/`v2`, enables OpenAI edit/regenerate/style-copy actions, and sends selected image context.
- History cards display workflow label plus `v1`/`v2` and never show raw provider/model names as product labels.
- No shadcn/registry files are added.
</success_criteria>

<output>
After completion, create `.planning/phases/10-provider-aware-result-continuation/10-01-SUMMARY.md`.
</output>
