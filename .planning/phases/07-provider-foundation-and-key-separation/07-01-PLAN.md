---
phase: 07-provider-foundation-and-key-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - packages/shared/src/types/index.ts
  - apps/api/src/lib/queue.ts
  - apps/api/src/services/generation.service.ts
  - apps/api/src/routes/generation.routes.ts
autonomous: true
requirements:
  - OPS-02
  - OPS-03
  - OPS-04
user_setup: []
must_haves:
  truths:
    - "Generation records persist provider, providerModel, and provider-specific trace fields before worker execution begins"
    - "ApiKey records carry a first-class provider dimension with Gemini-safe defaults for existing rows"
    - "Generation queue payloads include provider and providerModel, not just mode"
    - "Create and regenerate flows persist and re-enqueue the saved provider/model instead of defaulting silently"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Provider-aware Generation and ApiKey schema"
      contains: "enum Provider"
    - path: "packages/shared/src/types/index.ts"
      provides: "Shared provider/model contracts"
      contains: "GenerationProviderEnum"
    - path: "apps/api/src/lib/queue.ts"
      provides: "Provider-aware GenerationJobData"
      contains: "providerModel"
    - path: "apps/api/src/services/generation.service.ts"
      provides: "Provider/model persistence and requeue orchestration"
      contains: "providerModel"
  key_links:
    - from: "apps/api/src/services/generation.service.ts"
      to: "apps/api/src/lib/queue.ts"
      via: "addGenerationJob payload includes provider and providerModel"
      pattern: "providerModel"
    - from: "apps/api/src/routes/generation.routes.ts"
      to: "apps/api/src/services/generation.service.ts"
      via: "create/regenerate payloads and responses carry provider/providerModel"
      pattern: "provider"
    - from: "packages/shared/src/types/index.ts"
      to: "apps/api/src/routes/generation.routes.ts"
      via: "shared provider enum and generation response shape"
      pattern: "GenerationProviderEnum"
---

<objective>
Establish the provider-aware schema and contract layer that every later Phase 7 plan depends on.

Purpose: make provider/model lineage durable before admin key separation or worker routing begins.
Output: provider-aware Prisma schema, shared types, queue payload contract, and generation create/regenerate persistence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md
@.planning/phases/07-provider-foundation-and-key-separation/07-RESEARCH.md
@.planning/phases/07-provider-foundation-and-key-separation/07-VALIDATION.md
@.planning/phases/07-provider-foundation-and-key-separation/07-PATTERNS.md
@apps/api/prisma/schema.prisma
@packages/shared/src/types/index.ts
@apps/api/src/lib/queue.ts
@apps/api/src/services/generation.service.ts
@apps/api/src/routes/generation.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider-aware Prisma and shared type contracts</name>
  <files>apps/api/prisma/schema.prisma, packages/shared/src/types/index.ts</files>
  <read_first>apps/api/prisma/schema.prisma, packages/shared/src/types/index.ts, .planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md, .planning/phases/07-provider-foundation-and-key-separation/07-RESEARCH.md</read_first>
  <action>Add a single provider enum with exact values `gemini` and `openai` and use it in both `Generation` and `ApiKey`. In `apps/api/prisma/schema.prisma`, implement D-01, D-02, D-03, D-04, D-10, D-11, and D-12 by adding `provider Provider @default(gemini)`, `providerModel String @default("gemini-3-pro-image-preview") @map("provider_model")`, `providerTrace Json? @map("provider_trace")`, `openaiRequestId String? @map("openai_request_id")`, `openaiResponseId String? @map("openai_response_id")`, `openaiImageCallId String? @map("openai_image_call_id")`, and `openaiRevisedPrompt String? @map("openai_revised_prompt")` to `Generation`; add `provider Provider @default(gemini)` plus `@@index([provider, isActive])` to `ApiKey` so existing `ApiKey` rows backfill to Gemini per D-02 and existing `Generation` rows backfill `providerModel = gemini-3-pro-image-preview` per D-04. In `packages/shared/src/types/index.ts`, export `GenerationProviderEnum = z.enum(['gemini', 'openai'])`, `GenerationProvider`, and extend generation/admin-facing shared contracts so provider and providerModel are first-class typed fields rather than loose strings. Do not introduce booleans like `isOpenAI`; use provider enums everywhere.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api type-check && pnpm --filter @mockup-ai/web type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` contains `enum Provider`
    - `apps/api/prisma/schema.prisma` contains `providerModel String @default("gemini-3-pro-image-preview")`
    - `apps/api/prisma/schema.prisma` contains `openaiRequestId`, `openaiResponseId`, `openaiImageCallId`, and `openaiRevisedPrompt`
    - `packages/shared/src/types/index.ts` contains `GenerationProviderEnum = z.enum(['gemini', 'openai'])`
  </acceptance_criteria>
  <done>Schema and shared types now describe provider/model lineage explicitly, with Gemini-safe defaults covering existing rows and future OpenAI metadata fields reserved.</done>
</task>

<task type="auto">
  <name>Task 2: Persist provider/model in create and regenerate flows and queue payloads</name>
  <files>apps/api/src/lib/queue.ts, apps/api/src/services/generation.service.ts, apps/api/src/routes/generation.routes.ts</files>
  <read_first>apps/api/src/lib/queue.ts, apps/api/src/services/generation.service.ts, apps/api/src/routes/generation.routes.ts, packages/shared/src/types/index.ts, .planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md</read_first>
  <action>Implement D-03 through D-05, D-17, D-18, and D-19 at the contract layer. In `apps/api/src/lib/queue.ts`, extend `GenerationJobData` with required `provider: 'gemini' | 'openai'` and `providerModel: string`. In `apps/api/src/services/generation.service.ts`, update `CreateGenerationInput` to accept optional `provider` and `providerModel`, default current existing product flows to `provider = 'gemini'` and `providerModel = 'gemini-3-pro-image-preview'`, persist both fields on `prisma.generation.create`, and include both fields in every `addGenerationJob(...)` call. Update `regenerate(...)` so it reads `original.provider` and `original.providerModel` from the saved generation and re-enqueues those exact values instead of recomputing or defaulting to Gemini. In `apps/api/src/routes/generation.routes.ts`, extend the POST request schema with optional `provider` / `providerModel`, keep current pages working by defaulting omitted values to Gemini in the service layer, and include `provider` plus `providerModel` in create/get/regenerate responses so downstream admin and result surfaces have stable lineage fields.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/lib/queue.ts` contains `provider:` and `providerModel: string` in `GenerationJobData`
    - `apps/api/src/services/generation.service.ts` contains `provider:` and `providerModel:` in both `prisma.generation.create` and `addGenerationJob(`
    - `apps/api/src/services/generation.service.ts` reads `original.provider` and `original.providerModel` in `regenerate(`
    - `apps/api/src/routes/generation.routes.ts` returns `provider` and `providerModel` in create/get/regenerate responses
  </acceptance_criteria>
  <done>Create and regenerate now save provider/model before queueing, and queue payloads carry the same lineage fields the worker will later validate.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Run Prisma schema push and verify provider defaults landed</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>apps/api/prisma/schema.prisma, package.json, apps/api/package.json</read_first>
  <action>After all schema edits are complete, run the schema synchronization commands in this exact order from the repo root: `pnpm --filter @mockup-ai/api db:push`, then `pnpm --filter @mockup-ai/api db:generate`. Immediately follow with `cd apps/api && npx prisma validate` and a read check that the generated Prisma client includes the new provider fields. If `db:push` prompts or fails because of incompatible schema state, resolve the schema locally and rerun until `db:push` exits 0; do not skip this task, because later plans would otherwise pass TypeScript while the live database is still missing provider columns.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api db:push && pnpm --filter @mockup-ai/api db:generate && cd apps/api && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @mockup-ai/api db:push` exits 0
    - `pnpm --filter @mockup-ai/api db:generate` exits 0
    - `cd apps/api && npx prisma validate` prints a valid schema result
    - `apps/api/prisma/schema.prisma` still contains the provider defaults after the push step
  </acceptance_criteria>
  <done>The live database schema and generated Prisma client match the provider-aware contract, eliminating false-positive type-only verification.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @mockup-ai/api type-check`
- `pnpm --filter @mockup-ai/web type-check`
- `pnpm --filter @mockup-ai/api db:push && pnpm --filter @mockup-ai/api db:generate`
- `cd apps/api && npx prisma validate`
</verification>

<success_criteria>
- Provider/model/OpenAI lineage fields exist in `Generation`
- `ApiKey` is provider-scoped at the schema level
- Queue payloads include provider and providerModel
- Create and regenerate flows persist and re-enqueue the saved provider/model
- Database schema has been pushed successfully before Phase 7 backend/service work continues
</success_criteria>

<output>
After completion, create `.planning/phases/07-provider-foundation-and-key-separation/07-01-SUMMARY.md`
</output>
