---
phase: 07-provider-foundation-and-key-separation
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - apps/web/src/lib/api.ts
  - apps/web/src/app/admin/api-keys/page.tsx
  - apps/web/src/app/admin/api-keys/AddKeyModal.tsx
  - apps/web/src/app/admin/api-keys/ApiKeyTable.tsx
  - apps/web/src/app/admin/dashboard/page.tsx
autonomous: true
requirements:
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - "Admin web API contracts include provider-aware key, dashboard, and generation metadata fields"
    - "`/admin/api-keys` shows Gemini and OpenAI as separate tabs with independent CRUD/activate flows"
    - "Add key modal inherits provider from the selected tab instead of asking for free-form provider input"
    - "Dashboard shows provider-scoped active key state rather than a single global active key"
  artifacts:
    - path: "apps/web/src/lib/api.ts"
      provides: "Provider-aware admin API client contracts"
      contains: "activeApiKeysByProvider"
    - path: "apps/web/src/app/admin/api-keys/page.tsx"
      provides: "Provider-tabbed admin key page"
      contains: "Gemini"
    - path: "apps/web/src/app/admin/dashboard/page.tsx"
      provides: "Provider-scoped active key dashboard display"
      contains: "activeApiKeysByProvider"
  key_links:
    - from: "apps/web/src/app/admin/api-keys/page.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "list/create/delete/activate requests include provider"
      pattern: "provider"
    - from: "apps/web/src/app/admin/dashboard/page.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "DashboardStats consumes activeApiKeysByProvider"
      pattern: "activeApiKeysByProvider"
---

<objective>
Update the admin web layer so provider separation is visible and actionable without changing the current Gemini experience.

Purpose: satisfy the UI half of OPS-01 and prepare admin monitoring contracts for provider/model support metadata.
Output: provider-aware API client types, provider-tabbed `/admin/api-keys`, and provider-scoped dashboard active key UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md
@.planning/phases/07-provider-foundation-and-key-separation/07-UI-SPEC.md
@.planning/phases/07-provider-foundation-and-key-separation/07-RESEARCH.md
@.planning/phases/07-provider-foundation-and-key-separation/07-PATTERNS.md
@.planning/phases/07-provider-foundation-and-key-separation/07-02-PLAN.md
@apps/web/src/lib/api.ts
@apps/web/src/app/admin/api-keys/page.tsx
@apps/web/src/app/admin/api-keys/AddKeyModal.tsx
@apps/web/src/app/admin/api-keys/ApiKeyTable.tsx
@apps/web/src/app/admin/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update admin API client contracts for provider-aware keys, dashboard, and monitoring metadata</name>
  <files>apps/web/src/lib/api.ts</files>
  <read_first>apps/web/src/lib/api.ts, .planning/phases/07-provider-foundation-and-key-separation/07-UI-SPEC.md, .planning/phases/07-provider-foundation-and-key-separation/07-RESEARCH.md</read_first>
  <action>In `apps/web/src/lib/api.ts`, add a provider type union `type AdminProvider = 'gemini' | 'openai'` and update all admin-facing interfaces and methods to match the backend contract from Plan 02 plus the future admin monitoring fields required by D-10 through D-14. Extend `AdminApiKey` with `provider`. Replace `DashboardStats.activeApiKeys` with `activeApiKeysByProvider: { gemini: { alias: string; callCount: number } | null; openai: { alias: string; callCount: number } | null }`. Extend `AdminGeneration` with `provider`, `providerModel`, `openaiRequestId`, `openaiResponseId`, `openaiImageCallId`, and `openaiRevisedPrompt`. Change `adminApi.listApiKeys(token, provider)`, `createApiKey(token, { provider, alias, apiKey })`, `deleteApiKey(token, provider, id)`, and `activateApiKey(token, provider, id)` to send the provider using the exact backend contract from Plan 02. Keep the shared `request<T>(...)` helper; do not hand-write new fetch wrappers.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/web type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/api.ts` contains `type AdminProvider = 'gemini' | 'openai'`
    - `apps/web/src/lib/api.ts` contains `activeApiKeysByProvider`
    - `apps/web/src/lib/api.ts` contains `providerModel` in `AdminGeneration`
    - `apps/web/src/lib/api.ts` contains provider-aware signatures for `listApiKeys`, `createApiKey`, `deleteApiKey`, and `activateApiKey`
  </acceptance_criteria>
  <done>The web admin client contract now models provider-scoped keys and support metadata explicitly, so admin UI code can render the correct provider lane without stringly typed hacks.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild `/admin/api-keys` as provider tabs with provider-inherited modal and row actions</name>
  <files>apps/web/src/app/admin/api-keys/page.tsx, apps/web/src/app/admin/api-keys/AddKeyModal.tsx, apps/web/src/app/admin/api-keys/ApiKeyTable.tsx</files>
  <read_first>apps/web/src/app/admin/api-keys/page.tsx, apps/web/src/app/admin/api-keys/AddKeyModal.tsx, apps/web/src/app/admin/api-keys/ApiKeyTable.tsx, apps/web/src/lib/api.ts, .planning/phases/07-provider-foundation-and-key-separation/07-UI-SPEC.md</read_first>
  <action>Implement the UI-SPEC contract for D-06 through D-09. In `page.tsx`, add selected-provider state with exact labels `Gemini` and `OpenAI`, fetch the active tab’s key list via `adminApi.listApiKeys(accessToken, provider)`, and keep confirm/add/delete/activate copy provider-specific, e.g. `{Provider} API 키 추가`, `{Provider} API 키 '{alias}'가 활성화되었습니다`. In `AddKeyModal.tsx`, add a required `provider` prop, render the title as `{Provider} API 키 추가`, and keep provider inherited from the selected tab rather than asking the user to type it. In `ApiKeyTable.tsx`, keep the existing columns but omit any provider column, because the selected tab is the provider context; active rows still hide `삭제` and `활성화`, empty states become `등록된 {Provider} API 키가 없습니다`, and action handlers keep passing the selected provider back up to `page.tsx`. Use the existing toast and `ConfirmDialog` patterns; do not add a new tab or toast library.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/web type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/admin/api-keys/page.tsx` contains `Gemini` and `OpenAI`
    - `apps/web/src/app/admin/api-keys/page.tsx` calls `adminApi.listApiKeys(accessToken,`
    - `apps/web/src/app/admin/api-keys/AddKeyModal.tsx` contains `{Provider} API 키 추가` behavior via a provider prop
    - `apps/web/src/app/admin/api-keys/ApiKeyTable.tsx` no longer renders a provider column and still hides actions for active rows
  </acceptance_criteria>
  <done>The API key page now behaves as two provider lanes with shared UI chrome, preserving Gemini while making OpenAI key management explicit and isolated.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the single active-key KPI with provider-scoped dashboard state</name>
  <files>apps/web/src/app/admin/dashboard/page.tsx</files>
  <read_first>apps/web/src/app/admin/dashboard/page.tsx, apps/web/src/lib/api.ts, .planning/phases/07-provider-foundation-and-key-separation/07-UI-SPEC.md</read_first>
  <action>Update `apps/web/src/app/admin/dashboard/page.tsx` so it no longer renders one global “활성 API 키” card. Consume `stats.activeApiKeysByProvider` from the API client and render provider-scoped active-key information exactly as the UI-SPEC requires: Gemini and OpenAI must be separate visible units, and a missing OpenAI key must not imply Gemini is also missing. Reuse existing KPI and admin token styles rather than introducing new components. The display may be two compact cards or one section with two rows, but the copy must clearly separate `Gemini` and `OpenAI` and show `활성 키 미설정` when a provider has no active key.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/web type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/admin/dashboard/page.tsx` contains `activeApiKeysByProvider`
    - `apps/web/src/app/admin/dashboard/page.tsx` contains both `Gemini` and `OpenAI`
    - `apps/web/src/app/admin/dashboard/page.tsx` contains `활성 키 미설정`
  </acceptance_criteria>
  <done>The dashboard no longer lies about one global runtime key and instead shows provider-specific active key state consistent with the new backend contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @mockup-ai/web type-check`
- Browser smoke check on `/admin/api-keys`
- Browser smoke check on `/admin/dashboard`
</verification>

<success_criteria>
- Admin API client methods pass provider explicitly
- `/admin/api-keys` uses provider tabs with provider-inherited modal copy
- Active and inactive row behavior remains intact inside each provider tab
- `/admin/dashboard` shows provider-scoped active key state instead of one global key
</success_criteria>

<output>
After completion, create `.planning/phases/07-provider-foundation-and-key-separation/07-03-SUMMARY.md`
</output>
