---
phase: 07-provider-foundation-and-key-separation
plan: 04
type: execute
wave: 4
depends_on:
  - 07-02
  - 07-03
files_modified:
  - apps/api/src/services/admin.service.ts
  - apps/api/src/routes/admin/generations.routes.ts
  - apps/api/src/routes/edit.routes.ts
  - apps/api/src/worker.ts
  - apps/web/src/components/admin/generation-table.tsx
  - apps/web/src/components/admin/generation-detail-modal.tsx
autonomous: true
requirements:
  - OPS-02
  - OPS-03
  - OPS-04
user_setup: []
must_haves:
  truths:
    - "Worker dispatch validates provider against the saved generation record before mode-specific execution"
    - "Retry and edit paths preserve saved provider/model lineage and never fall back silently across providers"
    - "Admin generation APIs return provider, model, and safe support identifiers for support/debugging"
    - "Admin monitoring UI shows provider/model and a safe support-info section without exposing secrets or raw vendor payloads"
  artifacts:
    - path: "apps/api/src/worker.ts"
      provides: "Provider-aware worker dispatch with mismatch failure"
      contains: "provider mismatch"
    - path: "apps/api/src/services/admin.service.ts"
      provides: "Provider-preserving retry and admin generation metadata payload"
      contains: "providerModel"
    - path: "apps/web/src/components/admin/generation-detail-modal.tsx"
      provides: "Safe support metadata UI"
      contains: "지원 정보"
  key_links:
    - from: "apps/api/src/services/admin.service.ts"
      to: "apps/api/src/worker.ts"
      via: "retryGeneration requeues provider/providerModel that worker validates"
      pattern: "providerModel"
    - from: "apps/api/src/routes/admin/generations.routes.ts"
      to: "apps/web/src/components/admin/generation-table.tsx"
      via: "admin generation payload includes provider/model"
      pattern: "providerModel"
    - from: "apps/api/src/routes/admin/generations.routes.ts"
      to: "apps/web/src/components/admin/generation-detail-modal.tsx"
      via: "safe support identifiers map to the support section"
      pattern: "openaiRequestId"
---

<objective>
Finish Phase 7 by making worker routing provider-aware and exposing safe provider/model support metadata in admin monitoring surfaces.

Purpose: satisfy OPS-02, OPS-03, and OPS-04 without breaking existing Gemini execution.
Output: provider-validating worker/retry/edit flows plus admin generation table/detail support metadata UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md
@.planning/phases/07-provider-foundation-and-key-separation/07-UI-SPEC.md
@.planning/phases/07-provider-foundation-and-key-separation/07-RESEARCH.md
@.planning/phases/07-provider-foundation-and-key-separation/07-PATTERNS.md
@.planning/phases/07-provider-foundation-and-key-separation/07-02-PLAN.md
@.planning/phases/07-provider-foundation-and-key-separation/07-03-PLAN.md
@apps/api/src/services/admin.service.ts
@apps/api/src/routes/admin/generations.routes.ts
@apps/api/src/routes/edit.routes.ts
@apps/api/src/worker.ts
@apps/web/src/components/admin/generation-table.tsx
@apps/web/src/components/admin/generation-detail-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make retry, edit, and worker execution preserve and validate provider lineage</name>
  <files>apps/api/src/services/admin.service.ts, apps/api/src/routes/edit.routes.ts, apps/api/src/worker.ts</files>
  <read_first>apps/api/src/services/admin.service.ts, apps/api/src/routes/edit.routes.ts, apps/api/src/worker.ts, apps/api/src/lib/queue.ts, .planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md</read_first>
  <action>Implement D-15, D-16, D-17, D-18, and D-19 across retry, edit, and worker execution. In `apps/api/src/services/admin.service.ts`, update `retryGeneration(id)` so it requeues the failed generation with the saved `generation.provider` and `generation.providerModel` instead of rebuilding a Gemini-only payload; keep `retryCount` increment and pending reset intact. In `apps/api/src/worker.ts`, load the generation record by `generationId` before any vendor call, compare `job.data.provider` against `generation.provider`, and fail with the exact user-facing Korean message `저장된 provider와 큐 provider가 일치하지 않아 작업을 실행할 수 없습니다.` when they differ. Fetch the active API key with `getActiveApiKey(generation.provider)`, then dispatch by `provider` first and `mode` second, explicitly treating `Generation.provider` as the source of truth per D-17 and the queue provider as a validated copy per D-18. Keep the current Gemini implementation inside the `gemini` branch. For `openai`, satisfy D-16 by never falling back to Gemini; throw a clear unsupported-runtime error until later phases add the actual OpenAI image runtime. In `apps/api/src/routes/edit.routes.ts`, preserve provider lineage from the selected generation and reject non-Gemini edits with a provider-specific error instead of calling `geminiService.generateEdit(...)` on OpenAI-origin images.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api type-check && cd apps/api && npx vitest run src/services/__tests__/admin.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/admin.service.ts` passes `provider` and `providerModel` to `addGenerationJob(`
    - `apps/api/src/worker.ts` contains `저장된 provider와 큐 provider가 일치하지 않아 작업을 실행할 수 없습니다.`
    - `apps/api/src/worker.ts` switches on provider before mode-specific execution
    - `apps/api/src/routes/edit.routes.ts` contains a non-Gemini provider guard instead of unconditional Gemini execution
  </acceptance_criteria>
  <done>Retry, edit, and worker execution now preserve saved provider/model lineage and fail fast on wrong-provider conditions instead of drifting back to Gemini.</done>
</task>

<task type="auto">
  <name>Task 2: Extend admin generation API payloads with provider/model and safe support identifiers</name>
  <files>apps/api/src/services/admin.service.ts, apps/api/src/routes/admin/generations.routes.ts</files>
  <read_first>apps/api/src/services/admin.service.ts, apps/api/src/routes/admin/generations.routes.ts, .planning/phases/07-provider-foundation-and-key-separation/07-UI-SPEC.md</read_first>
  <action>Extend the admin monitoring payload shape in `apps/api/src/services/admin.service.ts` and `apps/api/src/routes/admin/generations.routes.ts` so list/detail consumers receive exactly the safe support fields from D-10 through D-14. Update `ListGenerationsResult.generations` to include `provider`, `providerModel`, `openaiRequestId`, `openaiResponseId`, `openaiImageCallId`, and `openaiRevisedPrompt` as nullable fields, explicitly covering D-11. Populate them in the Prisma query mapping from the new `Generation` columns. Keep `promptData`, `options`, `errorMessage`, and `retryCount`, but do not add raw `providerTrace`, raw vendor response blobs, API keys, or image base64 to the response. Preserve the current `/api/admin/generations` route family and response envelope; only widen the data payload shape so the UI can expose the D-13 operational identifiers and still honor D-14 redaction rules.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/admin.service.ts` maps `provider` and `providerModel` in `listGenerations(`
    - `apps/api/src/services/admin.service.ts` maps `openaiRequestId`, `openaiResponseId`, `openaiImageCallId`, and `openaiRevisedPrompt`
    - `apps/api/src/routes/admin/generations.routes.ts` still returns the same route path while exposing the widened payload
    - `apps/api/src/services/admin.service.ts` does not return raw `providerTrace` in admin generation payloads
  </acceptance_criteria>
  <done>Admin generation APIs now expose only the safe provider/model/support identifiers needed for support and later OpenAI lineage work.</done>
</task>

<task type="auto">
  <name>Task 3: Render provider/model badges and safe support info in admin monitoring UI</name>
  <files>apps/web/src/components/admin/generation-table.tsx, apps/web/src/components/admin/generation-detail-modal.tsx</files>
  <read_first>apps/web/src/components/admin/generation-table.tsx, apps/web/src/components/admin/generation-detail-modal.tsx, apps/web/src/lib/api.ts, .planning/phases/07-provider-foundation-and-key-separation/07-UI-SPEC.md</read_first>
  <action>Update the admin monitoring UI to consume the Phase 7 payload from Task 2. In `generation-table.tsx`, add visible provider/model information either as separate columns or as one stacked cell whose badge text is exactly `Gemini` or `OpenAI` and whose model text preserves the exact stored value such as `gemini-3-pro-image-preview` or `gpt-image-2`. In `generation-detail-modal.tsx`, add a collapsed-by-default `지원 정보` section that renders provider, model, `openaiRequestId`, `openaiResponseId`, `openaiImageCallId`, and `openaiRevisedPrompt` when present. Give every copy button a Korean `aria-label`, keep retry behavior unchanged, and do not render raw `providerTrace` JSON, encrypted keys, or uploaded image bytes/base64. Reuse existing modal layout and local state; do not add a new accordion library.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/web type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/admin/generation-table.tsx` contains `Gemini` and `OpenAI`
    - `apps/web/src/components/admin/generation-table.tsx` renders `providerModel`
    - `apps/web/src/components/admin/generation-detail-modal.tsx` contains `지원 정보`
    - `apps/web/src/components/admin/generation-detail-modal.tsx` contains copy-button `aria-label` strings in Korean
    - `apps/web/src/components/admin/generation-detail-modal.tsx` does not render `providerTrace`
  </acceptance_criteria>
  <done>Admin monitoring now makes provider/model lineage visible and exposes only safe support identifiers, matching the Phase 7 UI contract and leaving Gemini retry behavior intact.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @mockup-ai/api type-check`
- `cd apps/api && npx vitest run src/services/__tests__/admin.service.test.ts`
- `pnpm --filter @mockup-ai/web type-check`
- Browser smoke check on `/admin/content`
</verification>

<success_criteria>
- Worker validates provider against the DB record before execution
- Retry and edit paths preserve provider/model or fail clearly on unsupported provider runtime
- Admin generation APIs expose provider/model/support identifiers without raw trace blobs
- Admin generation UI shows provider/model and safe support info consistent with the UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/07-provider-foundation-and-key-separation/07-04-SUMMARY.md`
</output>
