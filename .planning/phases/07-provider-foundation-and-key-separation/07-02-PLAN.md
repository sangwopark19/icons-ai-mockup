---
phase: 07-provider-foundation-and-key-separation
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - apps/api/src/services/admin.service.ts
  - apps/api/src/routes/admin/api-keys.routes.ts
  - apps/api/src/services/__tests__/admin.service.test.ts
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "Admin key CRUD and activation are scoped to a selected provider instead of one global key pool"
    - "Gemini and OpenAI can each have one active key without deactivating the other provider"
    - "Dashboard stats expose provider-scoped active key info instead of one singular active key"
    - "API-side tests cover provider isolation and separate activation behavior"
  artifacts:
    - path: "apps/api/src/services/admin.service.ts"
      provides: "Provider-scoped API key business logic"
      contains: "activeApiKeysByProvider"
    - path: "apps/api/src/routes/admin/api-keys.routes.ts"
      provides: "Provider-aware admin key route validation and handlers"
      contains: "z.enum(['gemini', 'openai'])"
    - path: "apps/api/src/services/__tests__/admin.service.test.ts"
      provides: "Provider isolation regression coverage"
      contains: "provider"
  key_links:
    - from: "apps/api/src/routes/admin/api-keys.routes.ts"
      to: "apps/api/src/services/admin.service.ts"
      via: "route handlers pass provider into every CRUD/activate call"
      pattern: "provider"
    - from: "apps/api/src/services/admin.service.ts"
      to: "apps/api/src/services/__tests__/admin.service.test.ts"
      via: "Vitest assertions for per-provider active keys"
      pattern: "provider"
---

<objective>
Refactor admin key management so Gemini and OpenAI are isolated provider lanes, not one shared global active-key system.

Purpose: satisfy OPS-01 and unblock the Phase 7 UI and worker routing plans with a provider-scoped backend contract.
Output: provider-aware admin service methods, provider-validating admin routes, and regression tests for separate activation behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md
@.planning/phases/07-provider-foundation-and-key-separation/07-RESEARCH.md
@.planning/phases/07-provider-foundation-and-key-separation/07-VALIDATION.md
@.planning/phases/07-provider-foundation-and-key-separation/07-PATTERNS.md
@.planning/phases/07-provider-foundation-and-key-separation/07-01-PLAN.md
@apps/api/src/services/admin.service.ts
@apps/api/src/routes/admin/api-keys.routes.ts
@apps/api/src/services/__tests__/admin.service.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make admin service methods and dashboard stats provider-scoped</name>
  <files>apps/api/src/services/admin.service.ts</files>
  <read_first>apps/api/src/services/admin.service.ts, .planning/phases/07-provider-foundation-and-key-separation/07-CONTEXT.md, .planning/phases/07-provider-foundation-and-key-separation/07-RESEARCH.md</read_first>
  <action>Implement D-06, D-07, D-08, and D-09 in `apps/api/src/services/admin.service.ts`. Change the API key method signatures to require `provider: 'gemini' | 'openai'`: `listApiKeys(provider)`, `createApiKey(provider, alias, rawKey)`, `deleteApiKey(provider, id)`, `activateApiKey(provider, id)`, `getActiveApiKey(provider)`, and `incrementCallCount(provider, id)`. Every Prisma lookup for API keys must include the provider in `where`, especially the activation transaction and active-key reads. Replace the singular dashboard field with `activeApiKeysByProvider: { gemini: { alias: string; callCount: number } | null; openai: { alias: string; callCount: number } | null }` and populate it by querying each provider separately inside `getDashboardStats()`. Preserve the Phase 4 lifecycle rules per D-08: active keys cannot be deleted, activation remains explicit, and `callCount` / `lastUsedAt` remain provider-scoped rather than global. Keep encrypted key stripping and clear missing-key errors, but make the error message provider-specific such as `Gemini API 키가 설정되지 않았습니다` and `OpenAI API 키가 설정되지 않았습니다`.</action>
  <verify>
    <automated>pnpm --filter @mockup-ai/api type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/services/admin.service.ts` contains `listApiKeys(provider`
    - `apps/api/src/services/admin.service.ts` contains `getActiveApiKey(provider`
    - `apps/api/src/services/admin.service.ts` contains `activeApiKeysByProvider`
    - Prisma API key lookups in `admin.service.ts` include `provider` in their `where` clauses
  </acceptance_criteria>
  <done>The admin service no longer models a global active API key and instead exposes provider-scoped key CRUD plus provider-keyed dashboard stats.</done>
</task>

<task type="auto">
  <name>Task 2: Require provider in admin key routes and add provider-isolation tests</name>
  <files>apps/api/src/routes/admin/api-keys.routes.ts, apps/api/src/services/__tests__/admin.service.test.ts</files>
  <read_first>apps/api/src/routes/admin/api-keys.routes.ts, apps/api/src/services/__tests__/admin.service.test.ts, apps/api/src/services/admin.service.ts</read_first>
  <action>In `apps/api/src/routes/admin/api-keys.routes.ts`, define `const ProviderSchema = z.enum(['gemini', 'openai'])` and use it in all handlers. Make `GET /api/admin/api-keys` require `?provider=gemini|openai`. Make `POST /api/admin/api-keys` accept `{ provider, alias, apiKey }`. Make `DELETE /api/admin/api-keys/:id` and `PATCH /api/admin/api-keys/:id/activate` require the same provider via query string and pass it into the service layer. In `apps/api/src/services/__tests__/admin.service.test.ts`, extend the existing API key tests so they prove D-07 through D-09: activating an OpenAI key does not deactivate the Gemini key, list queries stay provider-filtered, delete/activate errors reference the selected provider, and dashboard stats return `activeApiKeysByProvider.gemini` plus `.openai` independently. Preserve all existing Phase 4 assertions and only extend them with provider-specific cases.</action>
  <verify>
    <automated>cd apps/api && npx vitest run src/services/__tests__/admin.service.test.ts && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/admin/api-keys.routes.ts` contains `ProviderSchema = z.enum(['gemini', 'openai'])`
    - `apps/api/src/routes/admin/api-keys.routes.ts` reads `provider` in GET, POST, DELETE, and PATCH handlers
    - `apps/api/src/services/__tests__/admin.service.test.ts` contains assertions for both `gemini` and `openai`
    - `cd apps/api && npx vitest run src/services/__tests__/admin.service.test.ts` exits 0
  </acceptance_criteria>
  <done>The admin key API now requires a provider on every operation, and regression tests prove one provider’s activation does not mutate the other provider’s active key state.</done>
</task>

</tasks>

<verification>
- `cd apps/api && npx vitest run src/services/__tests__/admin.service.test.ts`
- `pnpm --filter @mockup-ai/api type-check`
</verification>

<success_criteria>
- API key CRUD and activation are provider-scoped end to end
- Dashboard stats expose provider-scoped active key data
- Provider-specific missing-key and delete-guard behavior is preserved
- API-side tests prove Gemini and OpenAI active keys do not interfere with each other
</success_criteria>

<output>
After completion, create `.planning/phases/07-provider-foundation-and-key-separation/07-02-SUMMARY.md`
</output>
